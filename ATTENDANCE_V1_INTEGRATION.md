# TrendOS Attendance V1.1 Hybrid

## الحالة الحالية
هذه النسخة قابلة للعمل بعد دمج ملفات الواجهة في `main` بدون انتظار نشر Apps Script جديد.

`attendance-v1.js` يحاول أولاً استخدام Backend أصلي باسم `attendanceV1`. إذا كان الـDeployment الحالي لا يحتوي هذا المسار، يتحول تلقائياً إلى Hybrid fallback يعتمد على APIs الموجودة بالفعل في TrendOS:
- `saveMatbagyNote` لحفظ لقطات حالة الدوام المركزية.
- `getMatbagyNotes` لاسترجاع آخر حالة وعرض Dashboard المدير.
- `getActivityLog` لحساب الأوردرات والبنود المكتملة بواسطة الموظف خلال اليوم.

## تجربة الموظف
- بداية اليوم إلزامية قبل التشغيل للموظفين غير الإدارة.
- Pause / Resume لإيقاف واستكمال عداد العمل.
- Rest يومي 30 دقيقة افتراضياً.
- تأكيد تواجد كل 30 دقيقة مع مهلة رد 10 دقائق.
- عدم الرد = «يحتاج مراجعة» فقط، بدون خصم أو عقوبة آلية.
- تنبيه استراحة الصلاة لبنها؛ الاستراحة لا تخصم من Rest ولا تسجل ممارسة دينية فردية.
- نهاية اليوم وحساب وقت العمل والتوقف والإنجاز.
- إشعارات المتصفح/Windows عند السماح بها وطالما TrendOS مفتوح.

## Dashboard المدير
حساب الإدارة لا يُطلب منه Start Day. تعرض الوحدة أحدث حالة مركزية لكل موظف: الحالة، وقت العمل، القسم، عدد الأوردرات، وأي حالة تحتاج مراجعة.

## أولوية التشغيل
«الطباعة على الطاير» تظل أعلى أولوية في TrendOS، والمنصة الحالية تحولها بالفعل إلى عاجل وتسليم نفس اليوم.

## البيانات
تم تجهيز الشيتات التالية أيضاً للبنية الأصلية المستقبلية:
- `سجل الدوام`
- `نبض الحضور`
- `إعدادات الدوام`

وفي Hybrid الحالي تُحفظ اللقطات المركزية داخل `نوت مطبعجي` بتصنيف `ATTENDANCE_V1` بالإضافة إلى LocalStorage للجهاز، حتى يمكن التشغيل على الـDeployment الحالي.

## Backend الأصلي الاختياري
الملف `attendance-backend-v1.gs` موجود كتطوير لاحق. عند توفر مسار نشر Apps Script يمكن دمجه في `Code.gs` وإضافة:

```javascript
else if (action === "attendanceV1") result = attendanceV1_(e);
```

بعد نشره ستتحول الواجهة تلقائياً إلى Backend الأصلي بدون تغيير تجربة المستخدم.

## حدود V1.1
- تنبيه الحضور يحتاج TrendOS مفتوحاً. غلق المتصفح ليس دليلاً على غياب الموظف ولا يؤدي لأي قرار وظيفي تلقائي.
- الإشعارات أثناء إغلاق التطبيق بالكامل تحتاج لاحقاً Windows tray app أو Web Push.
- لا توجد كاميرا أو تعرف وجوه في هذه النسخة.
