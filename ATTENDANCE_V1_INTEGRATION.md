# TrendOS Attendance V1

## الهدف
وحدة تشغيل للموظفين داخل TrendOS تشمل:
- بداية اليوم قبل بدء التشغيل.
- Pause / Resume لإيقاف واستكمال عداد وقت العمل الفعلي.
- Rest يومي بإجمالي 30 دقيقة افتراضيًا.
- تأكيد تواجد دوري كل 30 دقيقة افتراضيًا، مع مهلة رد 10 دقائق.
- عدم الرد يضع الحالة «يحتاج مراجعة» فقط؛ لا يوجد خصم أو قرار وظيفي آلي.
- تنبيه استراحة الصلاة حسب موقع الفرع، دون تسجيل أو تقييم ممارسة دينية فردية.
- نهاية اليوم وتثبيت إجمالي وقت التواجد ووقت العمل والتوقف.
- احتساب عدد الأوردرات والبنود المكتملة من سجل حركة الأوردرات.
- أولوية «الطباعة على الطاير» مثبتة في إعدادات التشغيل.
- إشعارات Windows/المتصفح عند السماح بها.

## الملفات
- `attendance-v1.js`: واجهة الموظف والتنبيهات والـ timers.
- `attendance-backend-v1.gs`: منطق Apps Script وتسجيل الجلسات والنبضات.
- `config.js`: يحمل `attendance-v1.js` تلقائيًا على فرع الميزة.

## شيتات البيانات
تم تجهيز الجداول التالية في ملف TrendOS:
- `سجل الدوام`
- `نبض الحضور`
- `إعدادات الدوام`

## دمج Apps Script
`attendance-backend-v1.gs` يعتمد على دوال TrendOS الحالية مثل `authorize_()` و `ss_()`.

أضف الملف إلى مشروع Apps Script أو ادمج محتواه في `Code.gs`، ثم أضف في `doGet(e)` قبل سطر `Action غير معروف`:

```javascript
else if (action === "attendanceV1") result = attendanceV1_(e);
```

لا يلزم مسار POST مستقل؛ `doPost` الحالي يحول باقي الـ actions إلى `doGet`.

## النشر
1. انشر Apps Script كـ Deployment جديد بنفس صلاحيات TrendOS الحالية.
2. حدّث `TREND_API_URL` في `config.js` إذا تغيّر رابط الـ Deployment.
3. ارفع ملفات فرع Attendance إلى GitHub Pages بعد الاختبار.
4. سجل الدخول بحساب موظف وليس حساب الإدارة.
5. يجب أن تظهر نافذة «ابدأ يوم العمل» قبل استخدام التشغيل.
6. اضغط بداية اليوم واسمح بإشعارات المتصفح/Windows.

## اختبار أساسي
- Start Day ينشئ صفًا في `سجل الدوام` وحدث `start_day` في `نبض الحضور`.
- Pause يوقف عداد العمل الفعلي.
- Resume يعيد تشغيل العداد.
- Rest يوقف العداد ويزيد استهلاك الراحة فقط؛ الحد الافتراضي 30 دقيقة.
- Prayer break يوقف وقت العمل، لكنه لا يستهلك رصيد Rest ولا يدخل في أي تقييم ديني.
- Presence check يظهر كل 30 دقيقة أثناء حالة `working` فقط.
- عدم الرد خلال 10 دقائق يسجل `missed_check` و«يحتاج مراجعة» بدون خصم آلي.
- End Day يغلق الجلسة ويثبت الوقت والإنجاز.

## مواقيت الصلاة
الـ backend يستعلم عن مواقيت اليوم لمدينة الفرع من AlAdhan باستخدام طريقة `Egyptian General Authority of Survey` (method=5). يجب السماح لـ Apps Script باستخدام `UrlFetchApp` عند أول تشغيل. يمكن تعديل الموقع من `إعدادات الدوام` عبر `PRAYER_LOCATION`.

> مواقيت API قد تختلف دقائق عن مسجد/جهة محلية؛ إذا كانت الدقة التشغيلية مطلوبة يمكن إضافة offsets داخل الإعدادات لاحقًا.

## حدود V1
- تأكيد الحضور الدوري يعمل طالما TrendOS مفتوح على جهاز الموظف. غلق المتصفح ليس دليلًا كافيًا على المغادرة ولا يؤدي لخصم آلي.
- للحصول على تنبيه حتى مع غلق TrendOS نحتاج V2: Windows tray app أو Web Push backend.
- النظام لا يستخدم كاميرا أو تعرف وجوه في V1.
