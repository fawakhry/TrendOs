(function(){
  'use strict';
  if(window.__TRENDOS_GO_LIVE_AUTOPILOT_V1__)return;
  window.__TRENDOS_GO_LIVE_AUTOPILOT_V1__=true;
  if(window.MATBAGY_GO_LIVE_AUTOPILOT_V1===false)return;
  const API_URL=String(window.TREND_API_URL||window.API_URL||'').trim(); if(!API_URL)return;
  const ui={root:null,modal:null,drafts:[],busy:false,timer:null};
  const txt=v=>String(v==null?'':v).trim();
  const esc=v=>txt(v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
  function state(){return window.trendosState||window.state||{};} function user(){return state().user||null;}
  function auth(extra){const u=user()||{};return Object.assign({username:u.username||u.name||'',token:u.token||''},extra||{});}
  function canReview(){const u=user()||{},role=txt(u.role).toLowerCase(),key=(txt(u.username||u.name)+' '+role).toLowerCase();return role==='admin'||role==='service'||role==='final'||key.includes('ضياء')||key.includes('diaa')||key.includes('رحم')||key.includes('revan')||key.includes('rivan');}
  async function api(op,extra){const q=new URLSearchParams(auth(Object.assign({action:'goLiveAutopilotV1',op:op},extra||{})));const r=await fetch(API_URL+(API_URL.includes('?')?'&':'?')+q.toString(),{cache:'no-store',credentials:'omit'});const d=await r.json();if(!d||d.success===false)throw new Error(d&&d.message||'تعذر الاتصال بـ Go-Live Autopilot');return d;}
  function styles(){if(document.getElementById('glaV1Style'))return;const s=document.createElement('style');s.id='glaV1Style';s.textContent=`#glaV1Btn{position:fixed;right:14px;bottom:68px;z-index:2147481050;border:0;border-radius:999px;padding:10px 14px;background:#111;color:#fff;font:bold 13px Tahoma;box-shadow:0 8px 26px rgba(0,0,0,.18);cursor:pointer}#glaV1Btn.hot{background:#b42318}#glaV1Modal{position:fixed;inset:0;z-index:2147483550;background:rgba(5,20,35,.72);display:none;direction:rtl;font-family:Tahoma,Arial,sans-serif}#glaV1Modal.open{display:block}.gla-shell{position:absolute;inset:5%;background:#f6f8fb;border-radius:18px;overflow:auto;padding:16px}.gla-head{display:flex;justify-content:space-between;align-items:center;gap:10px;position:sticky;top:-16px;background:#f6f8fb;padding:12px 0;z-index:2}.gla-actions{display:flex;gap:7px;flex-wrap:wrap}.gla-actions button{border:0;border-radius:9px;padding:8px 11px;font-weight:700;cursor:pointer}.gla-primary{background:#005bff;color:#fff}.gla-green{background:#087f5b;color:#fff}.gla-muted{background:#e7edf3;color:#123}.gla-card{background:#fff;border:1px solid #e0e6ed;border-radius:13px;padding:12px;margin:9px 0}.gla-card.hot{border-right:5px solid #d92d20}.gla-card.ok{border-right:5px solid #039855}.gla-card.done{border-right:5px solid #667085}.gla-row{display:flex;gap:10px;justify-content:space-between;align-items:flex-start;flex-wrap:wrap}.gla-meta{font-size:12px;color:#667085;line-height:1.65}.gla-money{font-weight:800;font-size:17px}.gla-block{color:#b42318;font-size:12px;margin-top:7px}.gla-empty{text-align:center;color:#667085;padding:28px}@media(max-width:650px){#glaV1Btn{right:8px;bottom:70px} .gla-shell{inset:0;border-radius:0}}`;(document.head||document.documentElement).appendChild(s);}
  function build(){if(!canReview()||ui.root)return;styles();const b=document.createElement('button');b.id='glaV1Btn';b.textContent='🧾 فواتير الجاهز';b.onclick=open;document.body.appendChild(b);ui.root=b;const m=document.createElement('section');m.id='glaV1Modal';m.innerHTML=`<div class="gla-shell"><div class="gla-head"><div><h2 style="margin:0">جاهز → فاتورة → واتساب</h2><small>لا يتم ترحيل حساب مالي من موظف الإنتاج؛ التقفيل هنا فقط بعد اعتماد السعر.</small></div><div class="gla-actions"><button class="gla-primary" data-gla="sweep">تجهيز الجاهز</button><button class="gla-muted" data-gla="refresh">تحديث</button><button class="gla-muted" data-gla="close">إغلاق</button></div></div><div data-gla="list"><div class="gla-empty">جاري التحميل...</div></div></div>`;document.body.appendChild(m);ui.modal=m;m.addEventListener('click',click);refresh();}
  async function backgroundSweep(){if(!user()||!user().token)return;try{await api('sweepReady',{limit:40});if(canReview())await refresh();}catch(e){}}
  async function open(){ui.modal.classList.add('open');await backgroundSweep();await refresh();}
  async function refresh(){if(ui.busy||!canReview())return;ui.busy=true;try{const d=await api('listDrafts',{limit:100});ui.drafts=d.drafts||[];render();}catch(e){if(ui.modal)ui.modal.querySelector('[data-gla="list"]').innerHTML='<div class="gla-empty">'+esc(e.message)+'</div>';}finally{ui.busy=false;}}
  function render(){if(!ui.modal)return;const list=ui.modal.querySelector('[data-gla="list"]'),openRows=ui.drafts.filter(d=>d.status!=='تم التقفيل'||d.messageStatus!=='تم الإرسال');if(ui.root){const n=openRows.length;ui.root.textContent='🧾 فواتير الجاهز'+(n?' • '+n:'');ui.root.classList.toggle('hot',n>0);}if(!ui.drafts.length){list.innerHTML='<div class="gla-empty">لا توجد مسودات جاهزة حاليًا.</div>';return;}list.innerHTML=ui.drafts.map((d,i)=>{const blocked=d.status==='يحتاج تسعير/اعتماد',done=d.status==='تم التقفيل',cls=blocked?'hot':done?'done':'ok';return `<div class="gla-card ${cls}"><div class="gla-row"><div><b>أوردر ${esc(d.orderId)} • ${esc(d.customerName||'عميل')}</b><div class="gla-meta">${esc(d.phone||'')} • حالة الأوردر: ${esc(d.orderStatus||'-')}<br>المسودة: ${esc(d.status||'-')} ${d.invoiceNo?'• فاتورة '+esc(d.invoiceNo):''}<br>واتساب: ${esc(d.messageStatus||'لم تُرسل')}</div></div><div><div class="gla-money">${Number(d.subtotal||0).toFixed(2)} ج</div><div class="gla-meta">مدفوع مقترح ${Number(d.paidSuggested||0).toFixed(2)} • باقي ${Number(d.remainingSuggested||0).toFixed(2)}</div></div></div>${blocked?'<div class="gla-block">⚠️ '+esc(d.blocker||'راجع السعر والاعتماد')+'</div>':''}<div class="gla-actions" style="margin-top:9px">${!blocked&&!done?'<button class="gla-green" data-gla="finalize" data-i="'+i+'">تقفيل + إرسال واتساب</button>':''}${done&&d.messageStatus!=='تم الإرسال'?'<button class="gla-primary" data-gla="send" data-i="'+i+'">إرسال رسالة الجاهزية</button>':''}<button class="gla-muted" data-gla="prepare" data-i="${i}">إعادة فحص السعر</button></div></div>`;}).join('');}
  async function finalize(i){const d=ui.drafts[i];if(!d)return;let paid=prompt('المدفوع المؤكد للفاتورة رقم أوردر '+d.orderId,d.paidSuggested||0);if(paid===null)return;paid=Number(String(paid).replace(/[^0-9.\-]/g,''));if(isNaN(paid)||paid<0)return alert('قيمة المدفوع غير صحيحة.');let method='آجل';if(paid>0){method=prompt('طريقة الدفع: نقدي / InstaPay / Vodafone Cash / تحويل', 'نقدي');if(method===null)return;}if(!confirm('تأكيد تقفيل فاتورة أوردر '+d.orderId+' وإرسال رسالة الجاهزية للعميل؟'))return;try{const out=await api('finalizeAndNotify',{orderId:d.orderId,paid:String(paid),paymentType:method,send:'1',requestId:'GLA-FINAL-'+d.orderId});alert(out.message||'تم.');await refresh();}catch(e){alert(e.message);await refresh();}}
  async function send(i){const d=ui.drafts[i];if(!d)return;try{const out=await api('sendReady',{orderId:d.orderId});alert(out.message||'تم إرسال الرسالة.');await refresh();}catch(e){alert(e.message);}}
  async function prepare(i){const d=ui.drafts[i];if(!d)return;try{await api('prepareReadyInvoice',{orderId:d.orderId,notes:'إعادة فحص من شاشة Go-Live'});await refresh();}catch(e){alert(e.message);}}
  async function click(e){const a=e.target&&e.target.dataset&&e.target.dataset.gla;if(!a)return;if(a==='close')ui.modal.classList.remove('open');else if(a==='refresh')refresh();else if(a==='sweep'){await backgroundSweep();await refresh();}else if(a==='finalize')finalize(Number(e.target.dataset.i));else if(a==='send')send(Number(e.target.dataset.i));else if(a==='prepare')prepare(Number(e.target.dataset.i));}
  const wait=setInterval(()=>{if(user()&&user().token){clearInterval(wait);backgroundSweep();ui.timer=setInterval(backgroundSweep,5*60*1000);if(canReview())build();}},800);
  window.TrendGoLiveAutopilotV1={refresh:refresh,sweep:backgroundSweep};
})();
