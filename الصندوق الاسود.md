# الصندوق الاسود

> **الاسم الرسمي لذاكرة مشروع Trend Mall / TrendOS على GitHub.**
>
> هذا الملف هو المدخل الرئيسي لـ **الصندوق الاسود لترند مول** — الذاكرة التنفيذية الدائمة للمشروع.
>
> لا تستبدل أو تحذف ملفات الذاكرة التقنية الحالية؛ هذا الملف يجمعها ويشير إليها كمصدر واحد مترابط للحقيقة التنفيذية.

## ما هو الصندوق الاسود؟

الصندوق الاسود هو منظومة الذاكرة الكاملة لمشروع Trend Mall / TrendOS، ويحتفظ بـ:

- حالة المشروع الحالية.
- آخر نقطة توقف تنفيذية دقيقة.
- ما تم تنفيذه وما تم اختباره وما تم التحقق منه فعليًا.
- حالة الإنتاج الحالية والإصدارات النشطة.
- الـ rollback points.
- القرارات المعتمدة.
- الاختبارات ونتائج PASS / FAIL / PARTIAL.
- الـ checkpoints والأدلة التنفيذية.
- حدود الأمان وما هو ممنوع تغييره بدون موافقة.
- الخطوة التالية الدقيقة التي يجب استكمالها منها في أي محادثة جديدة.

## ترتيب القراءة الإجباري في أي محادثة TrendOS جديدة

1. `الصندوق الاسود.md`
2. `docs/trendos/TRENDOS_PROJECT_MEMORY.md`
3. `docs/trendos/TRENDOS_EXECUTION_LEDGER.md`
4. `docs/trendos/TRENDOS_HANDOFF.md`
5. `docs/trendos/TRENDOS_INTEGRITY_V1_DEPLOY_MANIFEST.md` عند أي عمل Deployment / Wiring.
6. `docs/trendos/TRENDOS_TEST_MATRIX.md` لنتائج الاختبارات والفشل التاريخي والـ regression gates.
7. `docs/trendos/TRENDOS_DECISIONS.md` للقرارات المعتمدة.
8. ملفات `docs/trendos/checkpoints/` للأدلة التنفيذية التفصيلية.

## قاعدة الحقيقة

عند وجود تعارض بين أي سجل قديم وجديد، تكون الأولوية:

`LATEST VERIFIED > EARLIER VERIFIED > DEPLOYED > TESTED > IMPLEMENTED > PREPARED > PLANNED > UNKNOWN`

لا يجوز اعتبار الخطة تنفيذًا، ولا الـ CI نجاحًا في الإنتاج، ولا الـ deployment تحققًا نهائيًا بدون Runtime Evidence.

## قواعد ثابتة

- لا تبدأ TrendOS من الصفر إذا كانت نقطة الاستكمال مسجلة هنا أو في الملفات المرتبطة.
- لا تطلب من المستخدم إعادة معلومات سبق توثيقها في الصندوق الاسود.
- لا يتم حذف تاريخ صحيح لمجرد وجود بيانات مكررة أو قديمة؛ يتم الحفاظ على الـ audit trail.
- `Order ID` هو المفتاح المنطقي للأوردر.
- `Line ID` هو المفتاح المنطقي للبند النشط.
- Google Sheets + Apps Script يظلان مصدر الكتابة المعتمد حاليًا حتى اكتمال واعتماد Cutover منفصل.
- Cloudflare / D1 حاليًا طبقة Read / Mirror / Performance ما لم يثبت الصندوق الاسود انتقال صلاحية الكتابة رسميًا.
- لا يتم استبدال Apps Script Production من GitHub `Code.gs` بشكل أعمى.
- أي خطوة Production مؤثرة يجب أن يكون لها Expected / Actual / PASS|FAIL + Rollback واضح.

## نقطة الاستكمال الحالية

المرجع التنفيذي النهائي لنقطة التوقف الحالية هو دائمًا:

`docs/trendos/TRENDOS_EXECUTION_LEDGER.md`

ويجب أن يتطابق معه:

- `docs/trendos/TRENDOS_PROJECT_MEMORY.md`
- `docs/trendos/TRENDOS_HANDOFF.md`

إذا اختلف أي ملف، يتم التوفيق باستخدام قاعدة الحقيقة أعلاه وتسجيل التصحيح في الـ Execution Ledger.

## تحديث 2026-09-04 — ربط Cloudflare بـ GitHub للتنفيذ الآلي

تم تثبيت الحقيقة التنفيذية التالية داخل الصندوق الاسود:

- مسار Cloudflare التجريبي مصمم ليُدار من GitHub بدل الاعتماد على Deploy يدوي متكرر من المستخدم.
- GitHub Actions workflow المعتمد لهذا المسار هو:
  - `.github/workflows/trendos-cloudflare-edge-preview.yml`
  - الاسم: `TrendOS Cloudflare Auto Preview`
- الـ Preview Worker المقصود هو:
  - `trendos-edge-gateway-preview`
- الـ workflow يعتمد على GitHub Secrets التالية:
  - `CLOUDFLARE_API_TOKEN`
  - `CLOUDFLARE_ACCOUNT_ID`
  - `TRENDOS_EDGE_SESSION_SECRET_PREVIEW`
- لا تُحفظ قيمة أي Token أو Secret داخل ملفات الذاكرة أو الكود أو المحادثات التنفيذية؛ يتم تسجيل الأسماء والغرض فقط، وتظل القيم داخل GitHub Secrets / Cloudflare secret storage.
- commit موثق لمسار الأتمتة:
  - `fae80c6b25428b257b9eacb0567de753dec4e47c`
  - `Automate Cloudflare preview on branch push`
- الهدف التنفيذي من هذا الربط هو السماح بتطوير واختبار Cloudflare Edge Gateway من فرع العمل ثم النشر إلى Preview تلقائيًا، مع بقاء Production بدون Cutover حتى اجتياز بوابات التحقق المنفصلة.
- هذا الربط لا يغيّر قاعدة أن Google Sheets + Apps Script هما سلطة الكتابة الحالية، ولا يمنح Cloudflare صلاحية كتابة authoritative تلقائيًا.
- عند استكمال مسار الأداء/Cloudflare لا نبدأ من إنشاء Token أو إعداد ربط يدوي من الصفر؛ نتحقق من حالة الـ workflow والـSecrets والـPreview الحالية أولًا ثم نكمل من آخر checkpoint موثق.

## تحديث 2026-09-04 — PERF-CF-02A Cloudflare Preview Safety PASS

تم تنفيذ أول بوابة فعلية في مسار الأداء/Cloudflare ونتيجتها **PASS**:

- تم اكتشاف وإغلاق خطرين قبل قبول الـPreview:
  - الـworkflow القديم كان يحتوي على `D1 migrations apply` على إعدادات `trendos-main`؛ أزيل من Preview.
  - Cloud-write health/handler كان يمكنه تهيئة schema قبل فحص kill switch؛ تم وضع fail-closed gate يجعل الصحة read-only ويمنع أي mutation بينما الكتابة OFF.
- تمت إضافة إعداد Preview مستقل في `cloudflare-d1/preview/wrangler.toml` باسم Worker `trendos-edge-gateway-preview`، مع `TRENDOS_CLOUD_WRITE_V1_ENABLED=false` وبدون migrations.
- تم اعتماد `cloudflare/wrangler-action@v3` داخل GitHub Actions بدل مسار `npx wrangler` الذي ثبت أنه يعلق في Runner.
- الـAuto Preview النهائي نجح بالكامل:
  - GitHub Actions run `33817812642`
  - job `100853724981`
  - verified head `748e7752be20f79f9ee2183ce20b9a9ec1102313`
  - Cloudflare Preview Version ID `1de87702-393c-4388-8070-63e2c36c0562`
- Runtime verification:
  - `/v1/edge/health` = HTTP 200، D1 available، auth configured، upstream configured، `cutover=false`.
  - anonymous `/v1/edge/whoami` = HTTP 401.
  - `/v1/cloud/write/health` = HTTP 200، `enabled=false`، `writesAccepted=false`، `schemaMutationFree=true`، `sheetsAuthoritative=true`.
  - `POST /v1/cloud/orders` بينما الكتابة OFF = HTTP 423 fail-closed.
- لم يحدث Production traffic cutover.
- Production Apps Script ما زال Version 146؛ Master+HEALTH ON فقط؛ كل Business families وFast Auth OFF؛ Apps Script + Sheets ما زالا سلطة الكتابة.
- لم يتم تغيير `config.js` أو GitHub Pages أو source Sheets أو Script Properties أو triggers أو Registry أو `Code.gs`.
- مسار CORE-P0 عند `3536-01` ما زال paused ولم يُستأنف.
- checkpoint التفصيلي:
  - `docs/trendos/checkpoints/PERF_CF_02A_CLOUDFLARE_PREVIEW_SAFETY_2026-09-04.md`
  - checkpoint commit `96906ababcb6b9fc3526d43d0b78404ae03e0ee7`

**الخطوة الحالية التالية:** `PERF-CF-02B` — قياس latency للـPreview/D1، والتحقق من D1 parity/freshness، ثم توسيع read path وpolling controls على فرع العمل فقط. لا Production cutover قبل بوابة منفصلة.

---

**الاسم المعتمد للمظلة بالكامل:** `الصندوق الاسود لترند مول`  
**GitHub entry point:** `الصندوق الاسود.md`
