# الصندوق الاسود

> **الاسم الرسمي لذاكرة مشروع Trend Mall / TrendOS على GitHub.**
>
> هذا الملف هو المدخل الرئيسي لـ **الصندوق الاسود لترند مول** — الذاكرة التنفيذية الدائمة للمشروع.
>
> لا تستبدل أو تحذف ملفات الذاكرة التقنية الحالية؛ هذا الملف يجمعها ويشير إليها كمصدر واحد مترابط للحقيقة التنفيذية.

## ما هو الصندوق الاسود؟

الصندوق الاسود هو منظومة الذاكرة الكاملة لمشروع Trend Mall / TrendOS، ويحتفظ بـ:

- حالة المشروع الحالية.
- آخر نقطة توقف تنفيذية دقيقة.
- ما تم تنفيذه وما تم اختباره وما تم التحقق منه فعليًا.
- حالة الإنتاج الحالية والإصدارات النشطة.
- الـ rollback points.
- القرارات المعتمدة.
- الاختبارات ونتائج PASS / FAIL / PARTIAL.
- الـ checkpoints والأدلة التنفيذية.
- حدود الأمان وما هو ممنوع تغييره بدون موافقة.
- الخطوة التالية الدقيقة التي يجب استكمالها منها في أي محادثة جديدة.

## ترتيب القراءة الإجباري في أي محادثة TrendOS جديدة

1. `الصندوق الاسود.md`
2. `docs/trendos/TRENDOS_PROJECT_MEMORY.md`
3. `docs/trendos/TRENDOS_EXECUTION_LEDGER.md`
4. `docs/trendos/TRENDOS_HANDOFF.md`
5. `docs/trendos/TRENDOS_INTEGRITY_V1_DEPLOY_MANIFEST.md` عند أي عمل Deployment / Wiring.
6. `docs/trendos/TRENDOS_TEST_MATRIX.md` لنتائج الاختبارات والفشل التاريخي والـ regression gates.
7. `docs/trendos/TRENDOS_DECISIONS.md` للقرارات المعتمدة.
8. ملفات `docs/trendos/checkpoints/` للأدلة التنفيذية التفصيلية.

## قاعدة الحقيقة

عند وجود تعارض بين أي سجل قديم وجديد، تكون الأولوية:

`LATEST VERIFIED > EARLIER VERIFIED > DEPLOYED > TESTED > IMPLEMENTED > PREPARED > PLANNED > UNKNOWN`

لا يجوز اعتبار الخطة تنفيذًا، ولا الـ CI نجاحًا في الإنتاج، ولا الـ deployment تحققًا نهائيًا بدون Runtime Evidence.

## قواعد ثابتة

- لا تبدأ TrendOS من الصفر إذا كانت نقطة الاستكمال مسجلة هنا أو في الملفات المرتبطة.
- لا تطلب من المستخدم إعادة معلومات سبق توثيقها في الصندوق الاسود.
- لا يتم حذف تاريخ صحيح لمجرد وجود بيانات مكررة أو قديمة؛ يتم الحفاظ على الـ audit trail.
- `Order ID` هو المفتاح المنطقي للأوردر.
- `Line ID` هو المفتاح المنطقي للبند النشط.
- Google Sheets + Apps Script يظلان مصدر الكتابة المعتمد حاليًا حتى اكتمال واعتماد Cutover منفصل.
- Cloudflare / D1 حاليًا طبقة Read / Mirror / Performance ما لم يثبت الصندوق الاسود انتقال صلاحية الكتابة رسميًا.
- لا يتم استبدال Apps Script Production من GitHub `Code.gs` بشكل أعمى.
- أي خطوة Production مؤثرة يجب أن يكون لها Expected / Actual / PASS|FAIL + Rollback واضح.

## نقطة الاستكمال الحالية

المرجع التنفيذي النهائي لنقطة التوقف الحالية هو دائمًا:

`docs/trendos/TRENDOS_EXECUTION_LEDGER.md`

ويجب أن يتطابق معه:

- `docs/trendos/TRENDOS_PROJECT_MEMORY.md`
- `docs/trendos/TRENDOS_HANDOFF.md`

إذا اختلف أي ملف، يتم التوفيق باستخدام قاعدة الحقيقة أعلاه وتسجيل التصحيح في الـ Execution Ledger.

## تحديث 2026-09-04 — ربط Cloudflare بـ GitHub للتنفيذ الآلي

تم تثبيت الحقيقة التنفيذية التالية داخل الصندوق الاسود:

- مسار Cloudflare التجريبي مصمم ليُدار من GitHub بدل الاعتماد على Deploy يدوي متكرر من المستخدم.
- GitHub Actions workflow المعتمد لهذا المسار هو:
  - `.github/workflows/trendos-cloudflare-edge-preview.yml`
  - الاسم: `TrendOS Cloudflare Auto Preview`
- الـ Preview Worker المقصود هو:
  - `trendos-edge-gateway-preview`
- الـ workflow يعتمد على GitHub Secrets التالية:
  - `CLOUDFLARE_API_TOKEN`
  - `CLOUDFLARE_ACCOUNT_ID`
  - `TRENDOS_EDGE_SESSION_SECRET_PREVIEW`
- لا تُحفظ قيمة أي Token أو Secret داخل ملفات الذاكرة أو الكود أو المحادثات التنفيذية؛ يتم تسجيل الأسماء والغرض فقط، وتظل القيم داخل GitHub Secrets / Cloudflare secret storage.
- commit موثق لمسار الأتمتة:
  - `fae80c6b25428b257b9eacb0567de753dec4e47c`
  - `Automate Cloudflare preview on branch push`
- الهدف التنفيذي من هذا الربط هو السماح بتطوير واختبار Cloudflare Edge Gateway من فرع العمل ثم النشر إلى Preview تلقائيًا، مع بقاء Production بدون Cutover حتى اجتياز بوابات التحقق المنفصلة.
- هذا الربط لا يغيّر قاعدة أن Google Sheets + Apps Script هما سلطة الكتابة الحالية، ولا يمنح Cloudflare صلاحية كتابة authoritative تلقائيًا.
- عند استكمال مسار الأداء/Cloudflare لا نبدأ من إنشاء Token أو إعداد ربط يدوي من الصفر؛ نتحقق من حالة الـ workflow والـSecrets والـPreview الحالية أولًا ثم نكمل من آخر checkpoint موثق.

## تحديث 2026-09-04 — PERF-CF-02A Cloudflare Preview Safety PASS

تم تنفيذ أول بوابة فعلية في مسار الأداء/Cloudflare ونتيجتها **PASS**:

- تم اكتشاف وإغلاق خطرين قبل قبول الـPreview:
  - الـworkflow القديم كان يحتوي على `D1 migrations apply` على إعدادات `trendos-main`؛ أزيل من Preview.
  - Cloud-write health/handler كان يمكنه تهيئة schema قبل فحص kill switch؛ تم وضع fail-closed gate يجعل الصحة read-only ويمنع أي mutation بينما الكتابة OFF.
- تمت إضافة إعداد Preview مستقل في `cloudflare-d1/preview/wrangler.toml` باسم Worker `trendos-edge-gateway-preview`، مع `TRENDOS_CLOUD_WRITE_V1_ENABLED=false` وبدون migrations.
- تم اعتماد `cloudflare/wrangler-action@v3` داخل GitHub Actions بدل مسار `npx wrangler` الذي ثبت أنه يعلق في Runner.
- الـAuto Preview النهائي نجح بالكامل:
  - GitHub Actions run `33817812642`
  - job `100853724981`
  - verified head `748e7752be20f79f9ee2183ce20b9a9ec1102313`
  - Cloudflare Preview Version ID `1de87702-393c-4388-8070-63e2c36c0562`
- Runtime verification:
  - `/v1/edge/health` = HTTP 200، D1 available، auth configured، upstream configured، `cutover=false`.
  - anonymous `/v1/edge/whoami` = HTTP 401.
  - `/v1/cloud/write/health` = HTTP 200، `enabled=false`، `writesAccepted=false`، `schemaMutationFree=true`، `sheetsAuthoritative=true`.
  - `POST /v1/cloud/orders` بينما الكتابة OFF = HTTP 423 fail-closed.
- لم يحدث Production traffic cutover.
- Production Apps Script ما زال Version 146؛ Master+HEALTH ON فقط؛ كل Business families وFast Auth OFF؛ Apps Script + Sheets ما زالا سلطة الكتابة.
- لم يتم تغيير `config.js` أو GitHub Pages أو source Sheets أو Script Properties أو triggers أو Registry أو `Code.gs`.
- مسار CORE-P0 عند `3536-01` ما زال paused ولم يُستأنف.
- checkpoint التفصيلي:
  - `docs/trendos/checkpoints/PERF_CF_02A_CLOUDFLARE_PREVIEW_SAFETY_2026-09-04.md`
  - checkpoint commit `96906ababcb6b9fc3526d43d0b78404ae03e0ee7`

**الخطوة الحالية التالية:** `PERF-CF-02B` — قياس latency للـPreview/D1، والتحقق من D1 parity/freshness، ثم توسيع read path وpolling controls على فرع العمل فقط. لا Production cutover قبل بوابة منفصلة.

## تحديث 2026-09-04 — Matbagy Multi-AI Room Architecture

اعتمد المستخدم اتجاه معماري جديد لمطبعجي: غرفة تواصل واحدة داخل المنصة تضم:

- المستخدم / Owner.
- ChatGPT.
- Gemini.

الهدف أن يصبح المستخدم طرفًا ثالثًا في نفس الغرفة مع نموذجين متخصصين، وليس تشغيل نموذجين منفصلين بلا ذاكرة مشتركة.

### الأدوار

- ChatGPT: `Design Orchestrator + Memory Manager + Workflow Planner + Final Synthesizer`.
- Gemini: `Visual Intelligence + Design Reviewer + Reference Comparator`.
- المستخدم: صاحب القرار والاعتماد النهائي.

### Orchestration

منصة مطبعجي هي الوسيط:

`User <-> Matbagy AI Orchestrator <-> ChatGPT`

`User <-> Matbagy AI Orchestrator <-> Gemini`

ولا يعتمد النظام على اتصال مباشر غير مضبوط بين النموذجين.

### أوضاع الغرفة المستهدفة

- `@GPT`
- `@Gemini`
- `@الكل` / `BOOM MODE`

المسار الافتراضي في BOOM MODE:

`User -> ChatGPT -> Gemini عند الحاجة البصرية -> ChatGPT synthesis -> User`

مع حد افتراضي `3` جولات AI-to-AI لمنع الحلقات اللانهائية، ثم يعود الدور للمستخدم أو تصدر خلاصة.

### الذاكرة والصور

- ذاكرة التصميم الرسمية: **صندوق مطبعجي** في Repository `fawakhry/Matbagy-Design-Workflow`.
- الصور الرسمية: Google Drive عبر `Case ID -> Asset ID -> Google Drive File ID`.
- ChatGPT وGemini لا ينشئان مصدر حقيقة موازٍ مستقلًا عن صندوق مطبعجي.

### حدود TrendOS

هذا القرار لا يغير سلطة الحقائق الحية:

- Order/Payment/Production/Inventory/Delivery facts تأتي من TrendOS live connectors/source-of-truth عند الربط.
- AI memory ليست مصدر حقيقة تشغيلية حية.
- Google Sheets + Apps Script يظلان سلطة الكتابة الحالية طبقًا للقواعد القائمة.

### الأمن

- OpenAI/Gemini API keys تحفظ Server-side فقط.
- ممنوع وضع المفاتيح في Frontend/browser أو داخل Prompt history.
- لا تمرر Secrets بين الـAgents.

### حالة التنفيذ

`APPROVED ARCHITECTURE / NOT YET IMPLEMENTED OR PRODUCTION-VERIFIED`

لا يوجد حتى الآن دليل Runtime على:
- API bridge فعلي بين OpenAI وGemini.
- واجهة غرفة مشتركة منشورة.
- Orchestrator منشور Production.

Cloudflare هدف مناسب محتمل لاستضافة الـOrchestrator لاحقًا، لكن يحتاج مسار تنفيذ واختبار مستقل ولا يؤثر على CORE-P0 أو Production cutover الحالي.

الوثيقة التفصيلية:

`docs/trendos/MATBAGY_MULTI_AI_ROOM_ARCHITECTURE.md`

## تحديث 2026-09-04 — PERF-CF-02B Freshness + Polling PASS

تم استكمال جزء مهم من مسار الأداء بدون Production cutover:

- D1 live mirror استعاد المزامنة قبل أي تعديل Production من هذا المسار.
- تم رفض الاكتفاء بلقطة واحدة، وتشغيل اختبار استقرار read-only عبر 4 دورات متتالية:
  - Workflow: `TrendOS Cloudflare Freshness Stability`
  - Run: `33820325930`
  - النتيجة: **SUCCESS**
  - Orders = `274/274`
  - Lines = `315/315`
  - عمر الـfreshness في العينات الأربع: `35s / 42s / 59s / 69s`
  - Orders وLines حافظا على نفس `syncedAt` في كل عينة.
- تم تجهيز Atomic Orders+Lines live-sync على فرع العمل مع staging/promote/observability، لكنه **لم يُنشر على Apps Script Production**.
- تم إضافة `trendos-poll-coordinator-v1.js` لمنع hidden-tab polling ودمج in-flight reads وفرض minimum intervals.
- تم تعديل مسارات الواجهة التالية على فرع العمل:
  - Operations Hub: إزالة synthetic focus storm.
  - Employee Manager: hidden-aware + coalesced + focus-throttled، مع forced refresh بعد write.
  - Press status: hidden-aware + coalesced + direct refresh API.
  - Customer Feedback: hidden-aware + local throttle فقط؛ لم يدخل generic read coordinator لأن `scan` قد يغيّر backend state.
- Attendance لم يتم تغييره في هذه المرحلة لأنه مسار حساس يجمع state/fallback/presence/prayer logic.
- اختبار CI جديد:
  - `tests/trendos_polling_coalescing_v1.test.mjs`
- CI النهائي:
  - Run `33821150092`
  - النتيجة: **SUCCESS**
  - جميع Integrity gates القديمة والجديدة PASS بدون regression.
- checkpoint التفصيلي:
  - `docs/trendos/checkpoints/PERF_CF_02B_FRESHNESS_POLLING_2026-09-04.md`
  - checkpoint commit `93aef46a65f10471f1feea1d2bdb23ec6243bf68`

**لا يزال ممنوعًا اعتبار هذا Production cutover.** لم يتم تغيير Apps Script Production أو config.js أو GitHub Pages أو صلاحية الكتابة authoritative.

**الخطوة التنفيذية التالية الدقيقة:** Auth/Fallback qualification على Preview + التحقق من تحميل coordinator/direct refresh في المتصفح + قياس request fan-out بعد التعديلات، ثم مراجعة Attendance منفصلة. Production read cutover يظل Gate مستقل يحتاج sustained freshness + authentication + fallback + rollback evidence.

## تحديث 2026-09-04 — PERF-CF-02C Edge Auth + Frontend Loading PASS

تم استكمال بوابة المصادقة والتحميل على مسار Preview/فرع العمل، بدون Production cutover:

- Preview auth qualification:
  - Workflow run `33821432424` = **SUCCESS**.
  - Worker Preview Version ID = `dd5407e6-80df-412f-89d6-1a71c032f0e5`.
  - anonymous `/v1/edge/whoami` = HTTP 401.
  - synthetic short-lived signed Edge token (subject `ci-preview-auth`) = accepted.
  - signed `/v1/edge/whoami` = PASS.
  - signed protected `/v1/edge/customer-manager/inbox?limit=1` = PASS with `dataSource=d1-edge`.
  - CI did not use or store any real employee credential.
  - empty `/v1/edge/session` = HTTP 400 before any upstream employee verification.
- Preview performance/freshness in the same run:
  - Edge health 15 samples: avg `154.5ms`, median `150.8ms`, p90 `182.7ms`, max `185.1ms`.
  - D1 Orders mirror read 15 samples: avg `271.9ms`, median `269.0ms`, p90 `299.8ms`, max `307.7ms`.
  - Orders = `274/274`, Lines = `315/315`, both age `31s` at final gate.
  - D1 full mirror = 87 sheets / 31,199 rows / 87 ready / 0 pending.
- Frontend loading contract on working branch:
  - `config.js` now loads `trendos-poll-coordinator-v1.js?v=20260904a`.
  - fresh cache tags added for Press / Customer Feedback / Employee Manager / Operations Hub polling-safe versions.
  - Operations Hub will not duplicate-load the coordinator if `config.js` already started loading it.
  - local module guards remain active if coordinator load is delayed.
  - `TREND_API_URL` and `API_URL` still point to Apps Script `WEB_APP_URL`.
  - `trendos-edge-read-v1.js` remains unloaded and `MATBAGY_EDGE_READ_V1_ENABLED` remains OFF.
- CI loading/coalescing regression gate:
  - run `33821589833` = polling loading/coalescing PASS plus all existing Integrity gates PASS.
- Follow-up Preview run after the working `config.js` change:
  - run `33821566883` = **SUCCESS** across safety/auth/freshness gates.
- detailed checkpoint:
  - `docs/trendos/checkpoints/PERF_CF_02C_EDGE_AUTH_FRONTEND_LOADING_2026-09-04.md`
  - checkpoint commit `c1a1c79d528144baca71fd48e0d363b59597e409`.

لم يتم في هذه المرحلة:
- نشر GitHub Pages Production.
- تغيير Apps Script Production.
- تفعيل Edge Read V1.
- نقل صلاحية الكتابة إلى Cloudflare.
- استخدام بيانات دخول موظف حقيقية في CI.
- استئناف CORE-P0.

**الخطوة التنفيذية التالية الدقيقة:** مراجعة Attendance polling بشكل منفصل، ثم تثبيت static request fan-out budget للواجهة، وبعدها فقط تجهيز read-cutover candidate صغير لـ Customer Manager inbox/thread مع Apps Script fallback وrollback switch. لا يُعتبر أي Preview PASS إذنًا تلقائيًا للـProduction cutover.

---

**الاسم المعتمد للمظلة بالكامل:** `الصندوق الاسود لترند مول`  
**GitHub entry point:** `الصندوق الاسود.md`
