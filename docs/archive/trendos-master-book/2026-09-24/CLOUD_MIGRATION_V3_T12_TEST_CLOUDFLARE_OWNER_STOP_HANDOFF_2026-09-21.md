# TrendOS — T12 Cloudflare isolated TEST Worker / owner stop & resumption checkpoint — 2026-09-21

**OWNER INSTRUCTION: STOP HERE.** This file records the complete Cloudflare TEST qualification session and the exact, unresolved live gate. Do not continue, automatically retry, promote, enable, or mutate any resource until the owner resumes. This supersedes the earlier provisional "TEST infrastructure not created / Hello World only" sections in prior checkpoints; use older docs for provenance, not as current state.

## Project scope and branch

- Repository: `fawakhry/TrendOs`.
- Isolated engineering branch: `cloud-migration-v3-t12-order-create-ci-20260919`.
- Initial target: Cloud-first eventual canonical Order/Line business write migration from the **original** Google Sheets/Apps Script to Cloudflare D1, but this checkpoint is **synthetic TEST qualification only**.
- Existing production: Sheets/Apps Script are still the business CREATE and lifecycle write authority; R5 Google-to-D1 periodic Orders/Lines mirror must not be disabled during preparation. Prior documented production Cloud Write V1 CW shadow is **NOT** T12 canonical numeric Order-create authority.
- Explicit owner consent covered a **new isolated TEST D1 + TEST Worker + synthetic-only qualification**; it did not authorize a live order cutover, existing prod Worker or D1 changes, actual customer/order copying, Script Properties deletion, changes to R5/Google writers, or disclosure of replay-backup/Secret material.
- The owner asked to stop and save **the entire state in this branch for a future conversation**. This document is the stop pointer. Current live Cloudflare state below is from owner-supplied screenshots and response text only, not independent authenticated Cloudflare introspection.

## TEST resources owner created manually

1. New Cloudflare D1 database **`trendos-t12-synthetic-test`**, TEST UUID `54a3c05e-cde9-4979-814f-d40f941edcd5`. Owner screenshots showed the newly created database initially had 0 tables, then a completed six-table list: `t12_synth_control`, `t12_synth_request_ledger`, `t12_synth_orders`, `t12_synth_lines`, `t12_synth_events`, `t12_synth_outbox`. Only these TEST schema objects, **not the production D1**, were used for manual SQL setup.
2. New standalone TEST Worker **`trendos-t12-synthetic-test`** at `https://trendos-t12-synthetic-test.trendmall-contact.workers.dev/`, separate from production `trendos-d1-api`. Owner first created Hello World and later bound exclusively the new TEST D1 with name `T12_SYNTHETIC_DB`. No screenshot evidence of any production `DB` binding to this Worker. Re-check bindings before further testing; screenshot != remote binding identity proof.
3. Owner ran TEST-only SQL for all six tables and inserted a **single** `t12_synth_control` fixture: `singleton=1`, `fixture_marker='T12_SYNTHETIC_ONLY'`, `google_writer_fenced=1`, `r5_mirror_writer_fenced=1`, TEST next numeric order `2001`. The two 'writer_fenced' flags are TEST database fixture flags, **not** an action to disable actual R5 or Google writers.
4. Owner read-only SQL reported, at the time of that query, `next_order_number=2001`, ledger/order/line/event counts all **0**. The separate `SELECT COUNT(*) FROM t12_synth_outbox` returned **0**. These are **historical pre-test measurements**, not a fresh post-accidental-deploy live parity audit.
5. Owner configured TEST Worker vars `T12_SYNTHETIC_TEST_ATTESTATION='ISOLATED_T12_SYNTHETIC_ONLY'`, `T12_SYNTHETIC_TEST_ENABLED='false'` initially, and a hidden encrypted Secret named **`T12_SYNTHETIC_TEST_BEARER_SECRET`**. The owner states its saved value is >32 characters; its raw value was never shared and **must never be requested, logged, put in GitHub, screenshots or chat**. At later points the enabled flag was temporarily set to true for an authorization test and then set back to false in Settings. Dashboard variable saves generate new Version History entries; **current Settings values are NOT automatically evidence of the config of the active deployed version**.

## Code implementation & CI evidence

- Source components on isolated branch:
  - `cloudflare-d1/schema-prep/t12-cloud-native-synthetic-create-v1.sql` (TEST schema, not a production migration).
  - `cloudflare-d1/src/t12-cloud-native-synthetic-create.mjs`, `t12-cloud-client-key-admission.mjs` (fixture-gated single synthetic Order+Line+ledger+event+outbox transactional prototype; historic `co_*` keys must not allocate a new Cloud order).
  - `cloudflare-d1/t12-preview/t12-cloud-native-synthetic-test-worker.mjs` (standalone TEST handler) and dashboard-compatible **single-file** `cloudflare-d1/t12-preview/t12-dashboard-singlefile-test-worker.js`. Only paste the single file into this TEST Worker; never import/deploy it into production Worker.
  - Default-OFF gate checks `T12_SYNTHETIC_TEST_ENABLED` before touching DB/auth, then verifies only isolated TEST DB marker, attestation, absence of prod DB/config, Secret length, TEST bearer auth and fabricated-only payload. Importantly, a GET to the route returns `test-post-only` before the enabled check; root GET returns `test-route-not-found`. Neither is an auth/disabled check.
- GitHub synthetic SQLite tests and TEST-only Worker tests passed before the dashboard session. Editor initially reported 3 TypeScript diagnostics on the dashboard file; two manual TEST editor line edits fixed them: at bundle line 42 Arabic-digit replacement callback now returns `String('٠١٢٣٤٥٦٧٨٩'.indexOf(d))`; line 504 WebCrypto accessed via `(/** @type {any} */ (globalThis)).crypto?.subtle`. Branch single-file artifact contains these same two fixes. The shared non-T12 V2 canonical contract was briefly changed for the type fix and **restored** to meet the hard scope guard. Final isolated [GitHub Actions run 35594612613](https://github.com/fawakhry/TrendOs/actions/runs/35594612613), job `106316468831`, was SUCCESS. Local/mock CI != remote D1/create parity or business cutover approval.
- Owner pasted complete ~557-line dashboard bundle into TEST `worker.js`, fixed the three editor diagnostics, showed `No problems have been detected in the workspace`, and manually deployed the TEST-only implementation. The TEST route has been served remotely; this checkpoint **supersedes** earlier text claiming Hello World remains the only deployed code.
- Observed owner-generated responses: browser GET `/__t12/synthetic/order-create` produced JSON `test-post-only`; POST to the exact TEST route with `ENABLED=false` produced HTTP 423 and `{"success":false,"syntheticOnly":true,"productionAuthorized":false,"code":"test-disabled"}`. Another root `/` POST in the Quick Editor returned HTTP 200 Hello World during a confusing preview/deploy phase; POST to a nonmatching root/path returned `test-route-not-found` or 404. Always verify exact request URL and the actually active version, not editor selection or cached preview.
- First temporary TEST enabling and no-Authorization POST yielded `test-auth-not-configured`, **not** expected `test-unauthorized`. Before the latest Secret update was promoted, a missing/outdated Secret in the active version was a plausible cause, but **the cause was never confirmed**. The user rechecked exact Secret name and >32-char length and updated it. No raw Secret sent. Later results of 423 occurred with flag OFF and thus DO NOT prove the Secret works. **No successful HTTP 401 authorization-denial evidence after the latest Secret was promoted is recorded**. No authenticated fabricated Order-create POST, no remote test Order created, no observed remote transaction replay/rollback test. Do not claim Cloud native order creation passed remotely.
- During UI operations, user accidentally chose Deploy rather than Save at least twice, including once **while the TEST ENABLED variable was true**. Promoting a specific version requires checking its own config, not assuming the current Settings screenshot carries over.

## Exact latest deployment state at owner's stop screenshot

Owner's final screenshot of TEST **Deployments** (after one accidental deploy with TEST flag true and a later flag update) showed:
- **Active deployed version `a672734e`** (shown deployed `4 minutes ago` in screenshot, top "Active deployment"; it was created by `Updated variable: T12_SYNTHETIC_TEST_ENABLED`). Owner explicitly stated the version was deployed when TEST flag was **true**. Treat it as **POTENTIALLY ENABLED** until disproven by a fresh exact endpoint response. Do not assume dashboard Settings `false` applies to this active deployed version.
- Latest saved version **`cb92607f`** (shown `11 seconds ago`, also `Updated variable: T12_SYNTHETIC_TEST_ENABLED`) was **not listed as active/deployed** in that screenshot. It likely reflects the owner's subsequent return to `false`, but this value has **NOT** been explicitly verified on this specific VERSION. The assistant's last proposed next step was to verify `false` in Settings, then promote `cb92607f` to 100% **only after verifying it is the disabled version**, and then verify the active route returns 423 `test-disabled`. The owner **did not send a confirmation screenshot/result for that promotion**, and immediately instructed STOP and record this checkpoint. Therefore: **NO evidence `cb92607f` was promoted or the active TEST endpoint was re-locked.**
- Earlier TEST version history for context: `94fa204c` TEST-only code deploy; `20506921` Secret update, later promoted to active 100% per owner's screenshot; `fb3fe17b` old active after earlier flag update; then `a672734e` active and newer `cb92607f` saved latest. This is only the recorded UI chronology, not an independently verified version contents matrix.
- The screenshot displayed a traffic bar ending at `100%` while another metric nearby said `0%`; use the **Active deployment ID** and exact HTTP behavior, not that ambiguous visual percentage as evidence of which configuration is safe. No reliable post-stop live active deployment/traffic/flag proof exists.
- The TEST hostname is Internet-reachable. When the flag is true, code still requires TEST bearer, schema marker, isolated DB binding, fabricated payload checks, and unsupported-case checks; **those restrictions do not justify leaving it intentionally enabled**, especially because no successful remote auth proof exists. The separate production resources were not intentionally changed; current production R5 live health was not freshly verified in this session.

## Stop-state controls, operational priority on resume

**STOPPED before additional actions.** Do not quietly run CI, change GitHub, create new Cloudflare resources, call POST, promote, toggle or use the Secret under this request to record the checkpoint. No background monitoring was set up.

**FIRST when owner resumes:** resolve the potentially enabled deployed TEST version. Work only on TEST `trendos-t12-synthetic-test`. Have owner show fresh **Settings** value and **Deployments** active/latest ID. If `Settings=false` but active still `a672734e`, do **not** infer the deployed flag is off. Identify a version proven to carry `ENABLED=false` along with intended TEST binding, attestation and code; owner may promote that exact disabled version to TEST 100% after reviewing the version confirmation. The previous candidate was `cb92607f`, not guaranteed disabled until checked. If an unambiguous disabled version cannot be established, stop and resolve the configuration/deployment UI before any tests. A *single unauthenticated POST with body `{}` to the exact TEST route*, used **only after safe relock deployment**, should return HTTP **423** `test-disabled`. That confirms disabled mode at that instant; do not repeatedly invoke or claim the Secret check passed. Do not test with a bearer until authorized and no screenshots reveal it.
 
**SECOND, later and separately scoped:** only after confirmed active disabled state, review how a future controlled TEST enable/read-only auth refusal check can be executed safely (a POST with `{}` and no bearer should yield 401 `test-unauthorized` when Secret is correctly bound and enabled; `test-auth-not-configured` means do not proceed). Confirm enabled state off again with a fresh 423. For an authorized fabricated-only create exercise, prepare explicit synthetic payload, ensure DB counts and numeric counter preflight parity, keep all raw bearer material private, test one canonical TEST key and idempotent same-key replay without blind retries, then postflight all six table counts and canonical response, with clear unknown-outcome stop rules. **Do not treat this checkpoint as approval to run such future tests unprompted.**

## Nonnegotiable safety invariants

- No live Order/Line canonical Cloud cutover; Google business writer authority remains until separate owner-approved full end-to-end cutover with all four Google create entrypoints and lifecycle routes fenced, legacy replay continuity, R5 mirror writer fence at actual authority switch, one numeric ID allocator, audit/rollback plan.
- Do NOT call old `trendosReplayFixed150DeleteOnce20260919` or delete any V1908 properties: old exact-150 backup belongs to a DIFFERENT previously deleted cohort; later new150 private backup did **not** authorize deletion. No original Sheets, Apps Script, Script Properties, original R5 trigger, production Cloudflare D1/Worker change to be performed as part of TEST qualification.
- Owner never provided the TEST Secret; do not request its value in chat, put it into GitHub/CLI commands/screenshots or log it. Any API/auth test should be performed locally by owner in authorized context with private secret handling.
- Cloudflare TEST Worker dashboard uses a tab whose path contains `/production` for that **standalone TEST Worker**; this dashboard environment label does not mean the original `trendos-d1-api` production Worker was modified.
- This file is the explicit resumption anchor; when owner asks to continue later, read it first, verify today's live dashboard state from new owner evidence, and continue **from the relock gate**, not from initial database/Worker/schema setup or earlier old-version Secret diagnosis.
