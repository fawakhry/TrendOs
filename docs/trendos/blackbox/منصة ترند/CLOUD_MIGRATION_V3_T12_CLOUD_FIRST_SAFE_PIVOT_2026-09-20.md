# TrendOS T12 — Owner-requested Cloud-first migration pivot, 2026-09-20

Status: **CLOUD ENGINEERING STARTED ON ISOLATED T12 BRANCH; PRODUCTION GOOGLE STILL THE ONLY ORDER/LINE WRITE AUTHORITY. NOT A LIVE CUTOVER.**

## Why switch direction
After R5 Orders/Lines sync recovery, the owner asked to **start the move to Cloudflare/D1 rather than expand the temporary Google Sheets replay ledger**. This is consistent with the documented target architecture (Google authoritative now; D1-first reads already qualified under T11; future family-by-family Google-to-Cloud write cutover). The separately created owner-private Google Sheets R6 ledger remains synthetic-only; do not provision its production analogue as a precondition for beginning T12 Cloud qualification. The newly backed-up 150 historic Script Properties are NOT deletion-authorized.

## Existing T12 implementation reused, not rewritten
Existing isolated modules:
- `cloudflare-d1/src/t12-order-create-shadow-intent.mjs`, `t12-order-create-shadow-store.mjs`, `t12-order-create-preflight.mjs`, `cloudflare-d1/t12-preview/order-create-shadow-handler.mjs`, `cloudflare-d1/schema-prep/t12-order-create-shadow-v1.sql`: shadow/intent qualifications only, no business Order ID or production route.
- `t12-order-id-authority-candidate.mjs` and schema-prep sequence are explicitly isolated/test-only, with no independent D1 business ID allocator while Apps Script creates Orders.
- `t12-order-create-cutover-state.mjs` is a pure engineering-readiness decision object; it **never** activates Cloud writes by itself.
- T11 existing D1-first reads and Apps Script fallback remain until a separately qualified Cloud authoritative read parity and fallback contract exists.

## New isolated implementation in this checkpoint
Extended the existing `T12_CREATE_CUTOVER_REQUIRED_EVIDENCE` and authority gate with five explicit proofs before a readiness-for-owner-decision result:
1. `allGoogleCreateEntrypointsFenceQualified`: no hidden Apps Script/alternate UI/new webhook create route can allocate an Order ID simultaneously with Cloud.
2. `legacyReplayContinuityQualified`: past V1908 Script Property keys and both differently backed-up historical cohorts remain recognized; replaying an old request after Cloud switch MUST NOT create a second Order.
3. `stableClientRequestAcrossTimeoutQualified`: all clients reuse exact idempotency key after an unknown outcome, including page reload and force confirmation; a new key must not silently bypass duplicate protection.
4. `r5MirrorWriterFenceQualified`: before D1 becomes canonical for Order/Line writes, the current Google→D1 R5 mirrored writer must be fenced under a separately qualified sequence so it cannot overwrite authoritative Cloud rows. **Keep R5 running until the approved boundary; NEVER stop or restart it during preparation.**
5. `cloudReadAndFallbackParityQualified`: D1-native Order/Line reads and the Google-fallback policy after Google freezes must not serve stale or conflicting business data.

The existing cutover preflight explicitly remains `productionActivationAuthorized:false` even if every boolean is true. Synthetic tests now prove each missing proof blocks readiness; GitHub Actions isolated test run `35526238605`, job `106118776602`: **SUCCESS**, including T12 hard-scope guard. These self-reported booleans are a checklist, not evidence of actual live parity or an authorization to deploy.

## Practical migration order and dependencies
**Phase A — preparation, now:** keep original Sheets authoritative; keep R5 periodic mirrored Orders/Lines and T11 reads. Review current original-bound Apps Script create/ID/line/queue/debt functions (owner-supplied Code.gs selected functions match the older repo text signature, but the actual deployed Web App Version 155 and other Head .gs files have not been byte-verified). Audit client key generation in current deployed frontend, all Google business create entrypoints, the numeric counter Property and source/archived Orders/Lines and D1 snapshot consistency. Retain and privately protect all historic replay Properties/backups; no new production client data in isolated GitHub tests.

**Phase B — Cloud-only test environment:** use separate Cloudflare Worker/D1 resources or a verified locally isolated SQLite mock, with no production D1 bindings/secret export. Qualify full Cloud-native idempotent Order+Line atomic transaction and business side effects (auth/permissions, identity, debt, open-order reuse/age, multiple departments, existing Order numeric ID and next Line number, activity/queue, read-your-write). Synthetic shadow intents and isolated allocator tests are NOT yet a complete canonical create implementation. A separate owner-approved test environment and exact permission check are needed before any remote shadow run.

**Phase C — no-dual-writer owner-controlled switch:** only after all 12 original T12 + five added gates PASS on current deployed source, freeze ALL Google create entrypoints under the exact production lock/fence, drain in-flight old writes and reconcile unknown outcomes, pin the actual highest numeric Order ID and the source + archived context, capture a D1 baseline under no concurrent writes, prepare durable historic replay mapping, fence R5 Google→D1 writer **at the actual authority switch** so it cannot overwrite Cloud rows, activate exactly ONE D1 allocation/write owner, validate complete Order/Line/side-effect content with end-to-end new order and idempotent old-key replay, then establish a read/fallback policy that cannot serve stale Google data. A future rollback must reconcile each Cloud-accepted order before routing any new create to Google; never just flip the frontend back while D1 has accepted orders or restart R5 to overwrite D1.

**Phase D — after observed parity and explicit owner authorization:** migrate remaining Google business-write families one by one. Keep historical backup and source Sheets for audit/rollback as long as needed. Do not bulk-delete V1908 replay keys or the 13 V2 baseline chunks as a shortcut; clearing Code.gs source files does not free Properties quota. Independent accounting idempotency keys and Google numeric counter can keep consuming the limited store until their own authority is transferred/decommissioned.

## Immediate live availability and quota guard
Latest owner-provided aggregate **at 2026-09-20 16:09 local**: 598 Script Properties, approx 468895 UTF-8 key+value bytes, 538 V1908 entries totaling 352074 approx bytes; this is HISTORICAL, not current. A later owner-run new150 PRIVATE backup was verified but **deleted zero** properties. Owner-provided scheduled R5 run receipts (including 19:53 local) showed `success=true`, `postflightRowParityVerified=true`, `scheduledSyncDisarmed=false` at those instants; no promise about next run. Cloud cutover cannot immediately repair today's Google quota while Google remains the writer. Continue incident monitoring without accidental retries; a future quota emergency requires its own verified private backup, replay continuity and exact owner-approved mitigation, not blind deletion.

## Changes actually made and stop point
Changes here: exactly existing pure `t12-order-create-cutover-state.mjs` readiness keys, its isolated tests, and this checkpoint documentation on the T12 GitHub branch. No production Apps Script, original Google Sheet, R5 trigger/Worker, production D1/schema, frontend main, secrets, Script Properties or owner-private Drive backup were modified in this checkpoint. No new live Cloud route is enabled, no real order or customer values copied. Next engineering can proceed offline on the existing T12 branch with direct source matching; **a future production canary or authority transfer is a separate user decision** scoped to the exact implementation, freeze/rollback window and evidence.
