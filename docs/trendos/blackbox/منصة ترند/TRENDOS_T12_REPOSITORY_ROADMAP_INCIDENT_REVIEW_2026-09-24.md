# TrendOS T12 — repository / roadmap / incident / safety-lineage audit
Date recorded: 2026-09-24. Scope: GitHub READ ONLY + documentation only. No production test or mutation.

## 0. Provenance and limits

Repository `fawakhry/TrendOs`, isolated branch `cloud-migration-v3-t12-order-create-ci-20260919`, **HEAD BEFORE audit `32636eea18aaa453cf060f65a5bf75f720c452ff`**. GitHub recursive tree was fetched without truncation: **1170 tracked blob files, 8,498,049 reported bytes**, covering project root, `docs/trendos`, blackbox, checkpoints, `.github/workflows`, tests, Apps Script candidates, Cloudflare source/preview and config. Tree-wide enumeration is **NOT** a line-by-line functional review of all 1170 files. This audit read the canonical memory/roadmap/decision and handoff files, the production incident, the T11/T12 handoff, selected historical and current journal entries, and the actual relevant V2/R4/R5/mirror/Worker/read-fallback code on the specified branch. Earlier journal Entry 58 already records exhaustive UTF-8 static-file inventory of a DIFFERENT older 1150-file parent snapshot; that does not certify every current file or currently deployed production bytes.

Evidence precedence as prescribed by `الصندوق الاسود.md` and project memory: **LATEST VERIFIED > EARLIER VERIFIED > DEPLOYED > TESTED > IMPLEMENTED > PREPARED > PLANNED > UNKNOWN**. Owner screenshots/reports establish only what they visibly show and when; GitHub branch config and CI do NOT establish currently deployed Apps Script/Cloudflare bytes or data parity. A dated file's claim of "CURRENT" is not evergreen. The 2026-09-24 report is a cross-file reconciliation, not an operational go/no-go certification or authorization.

## 1. Canonical navigation and stale-document traps

- Root `الصندوق الاسود.md` is the overall entry point; `docs/trendos/TRENDOS_ROADMAP_2027-03-01.md` is the broad product plan; `TRENDOS_PROJECT_MEMORY.md`, `TRENDOS_EXECUTION_LEDGER.md`, `TRENDOS_HANDOFF.md` record older Core history. `docs/trendos/blackbox/منصة ترند/00_INDEX.md` expressly directs ACTIVE T12 continuation to the **newest T12 journal and continuation handoff FIRST**, superseding older T11 and Sep19 T12 stop points for T12 only.
- `TRENDOS_EXECUTION_LEDGER.md` and older `TRENDOS_HANDOFF.md`/Project Memory still label a Sep1–2 **Version 146 / RP-06 paused** state as "current"; later independent `TRENDOS_BLACKBOX_2026-09-13_RP07_FINAL_HEALTH_PASS_CLOSED.md` closes RP-07 at its observed Sep12 health snapshot. The Sep19 incident and owner later Apps Script Executions show **Version 155** (exact immutable deployed source still unverified). Do NOT use the old version/one-trigger/read freshness facts as live Sep23/24 truth.
- `00_INDEX.md` contains a Sep19 incident OPEN/cleanup-PENDING subsection; the full **later append-only incident record** proves exact owner-approved 150 replay-property deletion occurred once and later R5 start/failure. Do not repeat cleanup or ask for initial R0 checks on the strength of that old paragraph.
- `README_V1931.md` contains a historic generic instruction to replace Apps Script `Code.gs`; this must NEVER be applied blindly to the current original deployed project. A prior owner source export differs from the repo `Code.gs` and a claimed Version 155 getContent artifact was lossy-transcoded. Do not treat a repository file as a complete, byte-exact live Head/deployment replacement.
- The Sep1 `TRENDOS_GO_LIVE_2026-09-01_MASTER.md` and the Mar1 2027 V1 roadmap describe DIFFERENT historical internal go-live and targeted product launch contexts; neither alone proves the platform is now down, fully operational, or eligible for a new cutover. No invented deadline.

## 2. Project timeline and independent tracks

| Period / track | What was ACTUALLY recorded | Interpretation NOW |
|---|---|---|
| Aug30–Sep2 Core Integrity | Offline integrity modules, controlled Apps Script R4/Version 146 deployment, Health-only activation, RP-06 preview issues. | Historical. Later RP-07 runtime health closure and newer Versions supersede old "current" banners. |
| Sep4–Sep13 performance/Core | Preview/auth/freshness/fallback regressions; specific T11 D1-first read cutovers and RP-07 final `OPEN_CORE_P0_BLOCKERS=0` for Sep12 snapshot. | T11 Print/Laser/Press/Service D1-first + Apps Script fallback were qualified at their dated releases, not independently retested here. Business writes remain Google. |
| Sep10 onward owner roadmap | Operator Task is next product implementation after RP-07; Department Invoice + Material Shadow/Parity (Gaber LASER, Wael PRINT), Laser/Print accounting control, then RP-08. | Separate business feature track from incident mirror repair and from T12 future Order-create authority cutover. Tasks V3/T2 is tracked on another branch; do not silently merge tracks. |
| Sep19 production incident | Property storage quota error disrupted UI/sync; owner stopped two legacy Apps Script triggers. One-time backed-up 150 V1908 replay keys deleted by owner after exact preflight; UI recovered per owner. | The 150-key deletion happened ONCE; further deletion risks losing idempotent historic replay. Original quota cause/retention is not permanently fixed by this relief. |
| Sep19–20 R4 then R5 recovery | R4 limited one-shot and temp route later disabled; R5 10-minute periodic mirror activated, initially reported full Orders+Lines postflight parity and an automatic idle success. | Historical successful snapshot, NOT perpetual mirror freshness. Operational enrichment was separately paused and not restored by Orders R5. |
| Sep21 R5 incident | Owner's 03:13 receipt success/idle, 03:23 generic `R5_PERIODIC_ABORT_UNEXPECTED`, unverified parity, self-disarm; later zero visible Apps Script triggers. Isolated CI proved generic wrapper can mask either pre-POST no-write OR a successful synthetic POST followed by cleanup exception. | Root exception and live 03:23 commit disposition UNKNOWN. Do not rerun R5/start or infer no prior write from outward flags. |
| Sep21–23 separate TEST T12 | One owner-operated fabricated TEST CREATE HTTP 201 and same-key replay 200, disabled 423, tested fixture counts unchanged; 36-statement packed-CAS synthetic SQLite CI only. | Real Cloud-native CREATE not transferred; TEST CREATE is not D1 mirror repair or real Cloudflare packed-CAS qualification. User explicitly forbids recreating TEST DB/Worker/schema or repeating synthetic experiments. |
| Sep23 production mirror diagnosis | One Work-mediated read-only source preflight: 705/761 last rows including header; owner PROD D1 catalog/actual: 652/708 at a separately observed time; production staging tables exist and both empty at inspection; owner later separate Work last rows 705/761 at 20:45 Cairo. | Distinct observation times, old preflight found 11 changed +53 tail positions per tab =128 candidate ROW POSITIONS, not 128 orders or a current transaction image. D1 mirror NOT restored. |
| Sep23 Entries 147–152 | Workers Paid active in owner screenshot, `trendos-main` dashboard `Rows written=50` in displayed 24h, business Worker 99 invocations, zero configured Cron/Queue/Email triggers in viewed settings, original Apps Script Triggers owner-reported empty. | Some D1 row writes occurred in that window, but **which table/writer/endpoint and whether now active remain UNKNOWN**. "Rows written 50" counts rows, not 50 independent write operations, does not mean 50 missing source rows and is not a Paid hard daily cap. |

## 3. Current architecture and hard safety boundary

Existing business frontend/cloud path is GitHub Pages / Cloudflare D1-first reads for selected department screens, with Apps Script fallback; **Google Sheets/Apps Script remain the only established real business Order CREATE and numeric-ID authority**. A D1 raw mirror recovery is NOT permission to move Order CREATE, number allocator, archived tabs, operational enrichment, Task mutations, billing or accounting to Cloudflare. The proposed T12 Cloud authoritative-create route is isolated/default-off/NOT cut over. At least five possible Google business-number writer routes and historic replay/client-key continuity have been documented; repo-only Code.gs does not cover all deployed source files. No reset of `NEXT_ORDER_NO`, replay properties or V2 baseline.

Expected production D1 `trendos-main` binding was owner-verified previously; static `cloudflare-d1/wrangler.toml` on T12 branch shows R4/R5 flags `false` but this is **NOT** current deployed runtime variable/route evidence. Owner screenshot Worker short version `654a3c91` is consistent with an earlier R5 enable deployment record but short version alone cannot prove current bytes and values. D1 metric 50 rows written and Worker HTTP traffic are not attributable to the two current mirror tables.

## 4. Existing two-current-tab V2 full-replacement mechanism — exact code-based risk

Direct source `cloudflare-d1/D1_Orders_Live_Sync_V2.gs`:
- `d1OrdersLiveSyncV2Names_()` has exactly `الأوردرات`, `بنود الأوردرات` (NOT FOUR tabs / archive). `StageSnapshot_` sends up to 80 source rows/request via `/v1/import/sheet`, `FullSync_` stages both then `atomicAction='promote'`. This may replace the whole CURRENT two-tab mirror, not only historic 128 candidate positions.
- `d1OrdersLiveSyncTickV2()` can trigger full rebase automatically if baseline is missing/old, saves last-full and Script Properties baseline after remote promote and before any independent content readback, and its error handling may clear baseline. DO NOT call `TickV2` or V1 start as an apparently read-only probe.
- `cloudflare-d1/src/mirror.js` stage with `reset=true` DELETEs staged rows/catalog **by sheet_name** rather than scoping reset to the current run ID; concurrent/unknown staging jobs may interfere. A normal 80-row stage uses row-wise SQL statements. Workers Paid displayed in owner billing removes the particular Workers Free 50-queries/invocation obstacle, but does NOT prove actual deployed code, request duration/payload, actual capacity or economic cost.
- `promoteStagedSheets` prechecks staged catalogs then executes a 12-statement D1 batch for TWO sheets, including `DELETE FROM sheet_rows WHERE sheet_name=?` and `INSERT ... SELECT` for each. It does NOT enforce exact live old-row preimage CAS or a Google/D1 cross-system write fence at commit time; stage catalog count alone cannot assert source content identity or absence of an outside writer. DB.batch transactional promotion protects intra-batch visibility/rollback when applied as designed, **not** lost-update risks before/around it.
- Original R4 bounded candidate path max **64 total positions / 5 new positions per tab**; not suitable for historical 128/53-per-tab. Local 36-statement packed CAS prototype is isolated/unrouted/`productionWriteAuthorized:false` and NOT proven on actual Cloudflare D1. Do not raise caps or install/test it in PROD as a shortcut.

## 5. Cross-document contradiction and what we should NOT ask owner to do next

The preceding assistant's proposed next screenshot was the original Apps Script **Executions** list. That screen **was already inspected in detail** in T12 Entries 94–104, including the Sep21 R5 latest 03:23 error, prior idle success, self-disarm and synthetic ambiguity. A recent-24h Executions screen might be a distinct newer observation but is **not the first actionable missing gate to repeat automatically**, and no new UI steps should be requested while owner has expressly paused for repository-wide review. Cloudflare Workers Cron/Queue/Email absence also does not fence HTTP, Google, other Worker or manual D1 writers. Do not mistake repeated navigation/aggregate screenshots for actual mirror recovery. No R5 restart, full V2 tick, SQL production write, Worker deploy, baseline/property edits, trigger restore, TEST resource, fabricated order replay or Cloud CREATE authority cutover.

## 6. Actual required acceptance contract before any separately approved mirror write

A controlled recovery of ONLY the two CURRENT source tabs must independently establish, near the write window: exact original Google workbook and complete active deployed Apps Script/Worker source/route identity; source and live D1 catalog/rows coherent private snapshots (including row identities/dimensions/header and content hashes, not merely counts), stable source before/after and Google Order CREATE lock/quiet constraints; effective fencing or CAS against **all** competing D1 mirror writers including manual HTTP/other Workers, stale/unknown R5 outcomes and concurrent staging; currently available budget (Apps Script execution/Script Lock, private payload, DB query/runtime/rows-written/cost) and Script Properties headroom; staged-to-source content/shape check, specific two-tab all-row DELETE/reimport risk and fail-closed abort; an explicit one-shot user authorization for the exact transaction distinct from diagnostic consent; loss/timeout/unknown-commit GET-only reconciliation and **NO blind retry**; independent GET-only source-to-D1 full content parity postflight before any separate baseline, trigger or business-write-authority decisions.

Do not characterize any single historic row count, zero trigger list, Workers Paid subscription, zero staging occupancy, or 50 Rows-written graph as closing those combined gates. Preserve archived mirrors and all legacy business operations unchanged. If sufficient qualified existing-resource safe control cannot be found in deployed V2, STOP before any production write and prepare an explicit isolated reviewed alternative only with the owner's separate approval; no new resource/test by inference.

## 7. What this review ACTUALLY did and next checkpoint

Read-only GitHub tree+key code/historical evidence audit and this documentation only. No tests/CI rerun, work browser, Apps Script, Google Sheet, Cloudflare navigation/SQL, D1 write, new DB/table/Worker, deploy, trigger, Script Properties change, order number, R5 action or source-row read in this review. **Production two-current-tab D1 recovery remains NOT RUN / NOT VERIFIED.**

Next action while the owner-requested repo audit is active: finish/save this reconciled evidence report, append a new unique journal Entry 153 with baseline HEAD/source and recorded result/commit, update T12 continuation handoff to **PAUSE ALL OPERATIONAL UI STEPS** and point to this report. Before proposing any live action, identify the precise next missing gate from the report (prefer existing deployed-safe writer fence and coherent source/target plan over duplicate metrics/screenshots), present risks and approval boundary to owner; do not launch it in this audit turn.
