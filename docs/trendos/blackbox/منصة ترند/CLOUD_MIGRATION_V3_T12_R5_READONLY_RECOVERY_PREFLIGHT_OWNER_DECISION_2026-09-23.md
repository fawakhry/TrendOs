# T12/R5 — separate owner decision for a read-only snapshot after self-disarm (PREPARED, NOT RUN)
Date: 2026-09-23. Isolated branch `cloud-migration-v3-t12-order-create-ci-20260919`. No production action in preparing this file.

## Established, do not redo
- Owner's actual Apps Script R5 prior execution at Sep 21 ~03:13 AM reported idle full-row parity and no disarm; ~03:23 AM reported `R5_PERIODIC_ABORT_UNEXPECTED`, parity unverified and `scheduledSyncDisarmed:true`. Original thrown exception/stack was not logged; the generic code cannot identify it retrospectively.
- New isolated synthetic tests show the same exact generic log can result with **zero simulated D1 writes** (pre-acquisition exception) or **one confirmed simulated D1 write** followed by a cleanup exception. CI `35860994839` passed; this does NOT imply an owner live D1 write happened during the incident. Do not trust `mutationPerformed:false` as independent transaction attestation.
- Owner-confirmed main Google Workbook remains business Order CREATE/ID authority. Owner-reported last `NEXT_ORDER_NO=4308` is historic/non-frozen. Owner read real `trendos-main` D1 current Orders/Lines counts `652/708` and reported syncedAt Sep 20, while independent prior main Workbook had `698/754` rows (including headers). This **non-atomic** +46/+46 difference exceeds the isolated R5 periodic `maxGrowPerTab=5` ceiling; R5 does NOT sync archived tabs. Blindly recreating the trigger is neither a valid catch-up nor a safe diagnostic.

## Already prepared READ-ONLY snapshot helper — not an approval to execute
Repository `cloudflare-d1/t12-preview/t12-d1-targeted-recovery-preflight-readonly-20260919.gs`, function `trendosD1TargetedRecoveryPreflightReadOnly20260919` (existing repository source; do NOT create a competing one-off helper). Prior isolated test `tests/t12_d1_targeted_recovery_preflight_readonly.test.mjs` checks simulated stable/unstable source/catalog conditions and no mutation. The helper takes a Script Lock, reads the two authoritative current Google tabs and pages through actual Cloudflare D1 mirror using GET, compares row-level snapshots internally, calculates **aggregate** missing/changed/delta counts, checks catalog/source stability and source identity, and estimates baseline storage. It logs only aggregates, no row content, tokens, original customer records, raw Script Properties or replay backup. Its output explicitly sets `productionWriteAuthorizedByThisAudit:false` and `triggerRestartAuthorizedByThisAudit:false`.

**Operational cost:** This is more intensive than the one-property numeric reader. It can acquire/hold the same Script Lock used for business order creation while reading hundreds of rows and remote pages, potentially delaying live business writes or timing out if the project is busy. Running it requires explicit owner approval for the read-only execution **and a quiet-business time window**. If the function is absent from the actual bound project, adding its reviewed source in a SEPARATE temporary `.gs` file modifies Apps Script draft/Head (even without Deploy) and requires the owner's separate consent to that edit. A historic GitHub source/CI result does not prove immutable deployed V155/Head parity or current actual D1 binding.

## One conditional owner decision, not yet received
Ask the owner **once, in plain Egyptian Arabic**, whether they approve one owner-run READ-ONLY source-versus-D1 diagnostic during a business quiet period, acknowledging temporary Script Lock contention and conditional need to add a draft helper file only if absent. If owner declines or cannot guarantee a quiet window, stop at read-only evidence and do not execute. Do not create a recurring trigger, run `trendosR5StartPeriodicOrders20260920`, use legacy R4/V1/V2 sync, perform live POST/SQL writes, run fixed150 cleanup again, touch Script Properties, issue a new Order CREATE or deploy a Worker.

## IF approved — conditional safe implementation steps for next chat
1. Verify actual project identity and whether the exact named preflight helper is already selectable in the Apps Script editor. **Do not blindly duplicate a helper** that is already installed. If absent, separately identify the previously reviewed source file and have owner explicitly consent to create/save a separate temporary draft source file; keep original V155 deployment unmodified, do not edit `Code.gs`. If unexpected authorization prompts or function dependencies are missing, STOP without substituting a similar-sounding mutating helper.
2. Choose an owner-confirmed quiet-business interval; confirm no one depends on immediate Order CREATE for a few minutes. This is no-write but lock-holding and network-intensive. Run only the exact read-only function once; never auto-retry after timeout. Ask for **sanitized aggregate JSON only** or exact error code, NOT raw customer/row data, backup, Script Properties values, tokens, script/database IDs or full project URL.
3. Verify catalog/source stability and bounded aggregate differences; record failed/blocked results, timestamp and scope in canonical T12 journal, keeping `productionWriteAuthorized=false` and `triggerRestartAuthorized=false`. An unstable, truncated, quota-failing or ambiguous result requires STOP/independent reconciliation; no live repair is inferred.
4. Design and independently mock-test a separately authorized, bounded recovery transaction using exact expected preimages, stable complete source and D1 snapshots, and post-commit read-only parity. The legacy R4 delta endpoint's catalog-only preflight is not transactional row CAS; do NOT run it as a shortcut. The archive mirror needs a separately qualified recovery strategy. Only after controlled one-time catch-up, quota/permissions readiness, observed parity and a later explicit owner change approval may scheduled R5 be reconsidered.

**Current state:** Document PREPARED and owner consent for the proposed lock-holding read-only diagnostic NOT YET GIVEN. No Apps Script source, live project function, trigger, Google Sheet, Worker, D1 or actual business order was changed by preparation.
