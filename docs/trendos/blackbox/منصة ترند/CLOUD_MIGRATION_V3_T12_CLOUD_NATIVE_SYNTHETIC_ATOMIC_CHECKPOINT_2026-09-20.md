# TrendOS T12 — Cloud-native isolated atomic rehearsal checkpoint / 2026-09-20

**STATUS:** GITHUB-ONLY + LOCAL SYNTHETIC SQLITE PASS. NOT ROUTED, NOT CLOUD PRODUCTION, NOT AUTHORITATIVE ORDER CREATE, NOT AN R5 CHANGE. Owner instructed: continue engineering until the next exact owner decision; do not stop at unrelated micro-gates.

## Baseline and source provenance
- Isolated branch: `cloud-migration-v3-t12-order-create-ci-20260919`.
- The owner-provided original-bound `Code.gs`-like text attachment was privately inspected but NEVER uploaded to GitHub or copied into any Cloud table. It contains all four functions listed below at uploaded lines 5187, 5902, 6295, 11091 and all four actual `makeOrderId_` calls at 5220, 5932, 6324, 11193; this matches the repository baseline's route/allocator layout. The exact live deployed Web App **Version 155** and all OTHER original-bound .gs file overrides/routers remain unverified. This is scoped positive evidence, not production byte parity.
- **Four distinct Google numeric Order-ID writers (six public action names):**
  1. `createManualOrder_ / createManualOrder` (employee).
  2. `mbCreateOrder_ / createOrder,createMatbagyOrder,clientCreateOrder` (Matbagy/public client).
  3. `createCustomerPortalOrder_ / createCustomerPortalOrder`.
  4. `submitCustomerDraft_ / submitCustomerDraft`.
  All four invoke shared `makeOrderId_` and will collide with an independently allocated D1 numeric sequence if left active when D1 becomes authoritative. A cutover gate that fences only `createManualOrder_` is INSUFFICIENT.
- In addition, repository `Code.gs` routes `updateLine`, `bulkUpdateDepartmentStatusV1926`, `archiveDeliveredDepartmentV1926`, `restoreArchivedOrderV1931`, `markCustomerNotified`, and `deliverReadyPickupBulk` through Google business mutations. These are a **known minimum** of other Order/Line writers, not an exhaustive multi-file route inventory. If R5 Google→D1 mirror is stopped while these continue, D1 status/archive/read data becomes stale. If R5 is left unscoped while Cloud has started canonical Order writes, it could overwrite Cloud authoritative mirror rows. The entire Order/Line lifecycle needs a single authority design, not just CREATE.

## Implemented isolated synthetic Cloud transaction rehearsal
Files:
- `cloudflare-d1/schema-prep/t12-cloud-native-synthetic-create-v1.sql`: six `t12_synth_*` tables (exclusive fixture+two writer fences+numeric sequence, request ledger, Orders, Lines, event ledger, pending queue/outbox). No existing production or `cloud_write_*` tables referenced; outside `migrations/`.
- `cloudflare-d1/src/t12-cloud-native-synthetic-create.mjs`: only restricted registered Print, one item, normal priority, no fly/press/multi/debt/open-order/reuse; strict authenticated TEST actor and marker-gated TEST DB adapter. Requires ALL isolated synthetic flags plus physically present `T12_SYNTHETIC_ONLY` control row with both Google-writer and R5-writer fences. Never imports a production Worker route and never directly calls Google or Script Properties.
- One D1 `db.batch` proposal atomically increments a TEST-only numeric allocator and inserts committed TEST request replay + exactly one TEST order + one TEST line + one activity event + one outbox event, then re-reads all five linked records and the response before success. On any ambiguous batch error, read back EXACT request key + actor/payload and all dependent records; if all committed/verified, respond idempotently; otherwise fail closed with NO automatic retry. SQLite adapter serializes overlapping in-memory batch transactions and tests uniqueness/rollback. **This proves test code behavior only, NOT deployed D1 transaction semantics, exact live auth/debt/line/summary lifecycle parity, or production safety.**
- `cloudflare-d1/src/t12-cloud-client-key-admission.mjs`: legacy `co_*` and historic Script Property keys are *replay-only/stop*, never a NEW Cloud order. A synthetically isolated future namespace `cld1_<13digit-timestamp>_<16+ random>` may enter the TEST branch but returns `createAuthorized:false` independently; the synthetic caller must also pass exact one raw `clientRequestId` field with no alias/trim. The eventual trusted live API must prove server-side epoch/session issuance, exact key persistence after timeout/reload, and a private/verified lookup for old keys and BOTH historic backup cohorts. No live key format/cutover has been adopted. Unknown old `co_*` keys are NOT converted into new `cld1_*` keys or retried as new Orders.
- Tests `t12_cloud_native_synthetic_create.test.mjs`, `t12_cloud_client_key_admission.test.mjs`: 6 injected failure boundaries rollback the complete local transaction (including allocator); same-key replay and altered payload/actor conflict; simulated lost success after commit resolves by readback; eight overlapping submissions produce one synthetic order; wrong flags/schema/fixture/unsupported business cases reject without writes; legacy and alias ID blocks.
- `tests/t12_google_create_writer_inventory.test.mjs`, `t12_google_order_lifecycle_writer_inventory.test.mjs`: guard documented repository minimum of Google CREATE and lifecycle mutation routes. This is NOT a live script file inventory.
- `tests/t12_legacy_cloud_write_boundary.test.mjs`: differentiates old `/v1/cloud/orders` Cloud Write V1 (CW-prefixed shadow event/outbox model) from new canonical numeric Order authority. The currently enabled legacy Cloud Write V1 MUST NOT be routed/renamed as T12 cutover by accident.

## Live Cloud GET-only evidence, not authority
A fresh public read-only `GET /v1/cloud/write/health` for the existing production Worker returned `success=true,enabled=true,writesAccepted=true,cutover=false,sheetsAuthoritative=true,database=true,schemaReady=true,pendingOutbox=0` at the sampled time. This means the **legacy CW shadow lane** is enabled and its own health claims Sheets authority and NO cutover. `writesAccepted=true` is for that legacy lane only; it is not evidence that any T12 CREATE was accepted or that existing numeric business Order IDs moved to D1. Do not POST to `/v1/cloud/orders` to "test" T12, change V1 flags, or drain its outbox.

## Explicit new authority gates
Updated pure `t12-order-create-cutover-state.mjs` requires TWO new booleans in addition to the previous 19 planned proofs:
- `allGoogleOrderLineMutationEntrypointsFenceQualified`: every relevant Google status/line/portal/archive/delivery/other Order/Line write route is either safely frozen or covered by a formally proven row/epoch-scoped writer split with no overlapping D1 writes.
- `cloudNativeOrderLineLifecycleParityQualified`: Cloud-owned orders still support required post-create status, delivery, archive, restore, line extension and side effects, preserving customer/department/accounting permissions and read/fallback consistency. Neither flag is TRUE yet. Current isolated tests are a synthetic API qualification, not an end-to-end business workflow.

## Verification and failures corrected
Isolated [GitHub Actions run 35528468424](https://github.com/fawakhry/TrendOs/actions/runs/35528468424), job `106124692330`: **SUCCESS** including T12 hard-scope guard and all new atomic, legacy-key, four-writer, lifecycle-writer and V1-shadow boundary tests. Earlier new synthetic test iterations `35527535656` and `35527599159` failed on an impossible zero writer-fence fixture (SQL CHECK correctly rejected it), and a mock DB adapter that did not serialize eight overlapping `BEGIN` calls; fixture and mock adapter were corrected before later successful CI. The real SQLite injected 6-step rollback suite remains green. Failures were isolated to GitHub test code and **never touched production**.

## Work not yet done / no false assurance
- Real production Cloud Order CREATE and Order/Line lifecycle implementations are **NOT** available, qualified or routed; no production tests or real business records used.
- Real Cloudflare D1 transactional test (separately scoped dedicated TEST DB, verified different from production) and separate Worker route are not yet provisioned. The existing Cloud V1 Worker/DB must NOT be used as a TEST target.
- Deployed Apps Script Version155 source, all live .gs route/override inventory, external Matbagy/customer portal clients, frontend same-key on page reload, old replay ledger private migration and original Script Property quota remain unverified. An owner-private 150 replay backup exists; it did **not** delete 150 Script Properties, and old previous 150 backup is for a DIFFERENT already removed cohort.
- No change to original production Sheets, Apps Script deployments/Head, Script Properties, R5 trigger, production Cloudflare Worker, D1 schema/data/config, secrets, frontend main, or existing V1 Cloud Write flag/outbox was performed during this checkpoint.

## NEXT OWNER DECISION — isolated Cloudflare TEST infrastructure only
The next action that cannot be completed purely by GitHub/offline tests is to **provision a separate named TEST Cloudflare D1 database and isolated TEST Worker binding** under the owner's Cloudflare account, with no production D1 database ID, live worker route, real customers, secrets, or order data. This is a test-resource creation/provisioning decision, NOT authorization to activate Cloud Orders, freeze Google writers, remove replay Properties, or change R5. Before providing UI instructions or scripts that touch Cloudflare, require the owner's explicit agreement to create those isolated test resources and confirm they will use a NEW D1 database (not the current production one). The connected Cloudflare browser automation path remains unavailable; owner may perform the Cloudflare console setup manually if they approve. Do not invite them to paste Cloudflare tokens/secrets or production data.

After test-resource confirmation: deploy only a disabled-by-default isolated synthetic handler with explicit TEST-only DB marker, auth and separate Worker hostname; test in remote D1 with fabricated data, then obtain deployed Version 155 source and complete full business lifecycle parity/authority contract before any real-order production gate.
