# T12 — owner-only production D1 numeric evidence (READ ONLY; prepared, not executed)
Date: 2026-09-22. For the currently used TrendOS Cloudflare account **only**.

## What is prepared and what remains unverified
The four separately numbered `SELECT` statements are in `cloudflare-d1/t12-preview/t12-production-d1-aggregate-evidence-readonly-20260922.sql` on the current isolated T12 branch. Their local in-memory SQLite test and isolated CI have PASSED, but **neither the assistant nor the owner has run them against real Cloudflare D1 as a result of this preparation**. GitHub `wrangler.toml` references an intended database named `trendos-main` with binding `DB`; it is source configuration, **not** proof of active production account, deployment binding, database UUID, actual R5 mirror freshness, or order-number authority. `trendos-t12-synthetic-test` is an entirely different TEST database and must never be substituted.

## Single owner preflight before pasting ANY SQL
In your authenticated Cloudflare dashboard, identify the actual TrendOS business **production** Worker; open its currently active D1 bindings. Privately verify that the D1 binding named `DB` points to the correct business database `trendos-main` **in the same Cloudflare account**. Then navigate to THAT database's D1 SQL Console. Independently verify the database identity/account and that it is **NOT** `trendos-t12-synthetic-test`, a staging/preview DB or merely a TEST Worker labeled "Production" in its own deployment UI. If either linkage is ambiguous, STOP and ask for a non-secret, cropped screenshot of the Worker binding and database header; redact account/zone IDs, UUIDs, private URLs, secrets and all customer data. Never copy real database IDs into the GitHub journal.

## Run queries without modifying D1
Only after account, Worker binding and business D1 database match, copy the *body of QUERY 1* from the prepared SQL file into the D1 SQL Console and run it ALONE. It must return the three table names `orders`, `sheet_catalog`, `sheet_rows` (order unimportant); if a table is missing or Cloudflare reports any error, STOP. Never create a missing table, execute a migration, install Wrangler, or switch databases to make the query pass.

If QUERY 1 passes, run QUERY 2, 3 and 4 **one at a time, separately**. They return only: mirror catalog names/status/counts/last-sync times; normalized Orders count and maximum numeric ID; and first-column displayed mirror numeric count/max with invalid-json counts and sync timestamps for the four known tabs. Queries never return customer/order rows, raw IDs, `values_json`, `display_json`, `headers_json`, notes, billing details or access credentials. QUERY 2 must find all four expected production tabs; absent/duplicated/unready/stale catalog rows are evidence to record, **not** permission to repair or write.

Do not paste the whole SQL file if Console supports only one statement; previous TEST D1 Console rejected compound/multi-statement SQL. Do not select `SELECT *`, browse/export customer rows, run `INSERT`/`UPDATE`/`DELETE`, change any setting, re-enable scheduled R5, deploy a Worker, start Google writer freeze, or touch the original/test create/replay endpoints. If a run fails, record its query number and sanitized error text and STOP; do not improvise a fallback read/write.

## Share only sanitized factual output
Send only the four query result *aggregates* (table names, status, numeric counts/max and timestamps) and the observation time with timezone; if screenshots, crop to the query-result grid and mask Cloudflare account identifiers, database UUID, Worker URL, private tokens and any unrelated data. Even if current Google owner-reported number 4308 matches a D1 number, those online cross-system readings are not atomic. A stale catalog, missing archived tab, normalized-mirror mismatch, or different numeric max must be recorded as such rather than "corrected" using any write or fabricated snapshot.

This is a **baseline read** only. It cannot qualify `productionVersion155SourceExact`, `historicalLineIdentityMappingQualified`, `r5MirrorWriterFenceQualified` or `allocatorSeedPinned`; the final seed needs a **separately approved writer fence** and coherent post-fence reread, business parity and rollback. Google remains the exclusive real business CREATE/number authority. No execution of these owner-only SQL statements has been reported in this conversation as of this runbook.
