# TrendOS T12 — Owner-approved separate Cloudflare TEST resource provisioning / 2026-09-21

**Owner decision:** The owner expressly approved creation of ONE NEW isolated Cloudflare D1 TEST database and ONE NEW isolated TEST Worker for synthetic-only order-create qualification. This is NOT approval for production Cloud Order writes, real business/customer data copying, Script Properties deletion, R5 trigger change, Google writer freeze, release of old replay backups, or use of the existing production D1 binding.

## Tools / actual provisioning outcome
- Fresh TinyFish wallet read returned `-0.138766 USD` with auto-reload unconfigured and no pending top-up. An interactive Cloudflare browser run could not be responsibly initiated. Available connected account tools include Google Drive and GitHub, not an authorized Cloudflare D1/Worker management action. Plugin directory search for Cloudflare/Workers/D1 returned no Cloudflare management connector. No Cloudflare account creation request, dashboard click, API write, remote D1/Worker creation, binding or deployment occurred; **the owner must execute the two TEST resource creation actions in their own Cloudflare dashboard or via an independently authorized Work cloud-browser connection**. Do NOT claim approval alone created resources.
- Existing legacy `/v1/cloud/write/health` GET previously reported `cutover=false,sheetsAuthoritative=true` for the **existing** CW shadow lane. It must not be repurposed. `cloudflare-d1/wrangler.toml` remains the PROD Worker config; NEVER use it for TEST.
- New TEST-only template on isolated T12 GitHub branch: `cloudflare-d1/t12-preview/wrangler.t12-synthetic-test.template.toml` with `name='trendos-t12-synthetic-test'`, `database_name='trendos-t12-synthetic-test'`, binding `T12_SYNTHETIC_DB`, an intentionally invalid `<NEW_TEST_D1_DATABASE_ID>` placeholder, `T12_SYNTHETIC_TEST_ENABLED='false'` and TEST fixture attestation. Its production-free main points ONLY to `./t12-cloud-native-synthetic-test-worker.mjs`. Do not replace the placeholder with the existing PROD database UUID.
- New isolated static guard `tests/t12_synthetic_resource_config_guard.test.mjs` fails if the test template contains the production UUID, production `DB` binding or legacy production secrets/URL, or if the standalone TEST code is imported by production Worker. Isolated T12 CI run [35587304574](https://github.com/fawakhry/TrendOs/actions/runs/35587304574), job `106293493835`, **SUCCESS**. This is a local STATIC isolated CI check, NOT a remote Cloudflare test.

## Owner-executable creation steps — exact separate TEST resources
1. Log into the existing Cloudflare account. From **D1 SQL Database > Create Database**, create a **NEW** database named exactly `trendos-t12-synthetic-test`. If Cloudflare shows the production DB (used by `trendos-d1-api`) do NOT select, duplicate content from, edit, delete, migrate or reuse it. Capture the NEW DB UUID/URL locally; it is a resource identifier, not an API token or secret. If the proposed test name already exists, STOP and inspect its details rather than overwriting or assuming it is disposable.
2. In **Workers & Pages > Create application > Create Worker**, create an entirely NEW Worker named `trendos-t12-synthetic-test` in the same account, using a blank/Hello World starter only. Avoid linking or auto-deploying existing `fawakhry/TrendOs` production branch, `trendos-d1-api` or other production Worker. The starter may display a benign Hello World test page; it is NOT T12 logic and must NOT accept any order data. Do NOT yet paste/enable the staged T12 handler.
3. Keep the TEST Worker **without ANY D1 binding and without secrets** until its exact name/account and the new DB UUID have been independently checked against the production D1 UUID. Then specifically bind only the NEW DB as `T12_SYNTHETIC_DB`, never `DB`; implement schema, secret and default-OFF TEST handler in a later controlled step. The sole current instruction is CREATE resources, not schema execution nor live handler deployment.
4. Return a screenshot of each new resource's own detail page showing TEST names and new DB UUID (may redact account email and all API tokens); alternatively report only the two TEST resource names and the NEW DB UUID plus `created` status. Do NOT share Cloudflare API keys, bearer secrets, customer data, historical replay backup metadata, production D1 tokens or production DB SQL dump. An identifier alone is not a credential but should not be publicly reposted.

## Following resource evidence
- Independently verify test vs production UUID difference, test Worker route/name, no live binding/config overlap, and exact TEST-only schema. Do not apply `t12-cloud-native-synthetic-create-v1.sql` to the PROD database or reuse legacy CW shadow. Remotely test one fabricated restricted Print order, old-key refusal, same-key retry and lost-response readback before any discussion of production cutover.
- Future real-order Cloud authority requires separate exact owner gate for fully scoped cutover (Version155 source, all four Google create writers, all Google Order/Line lifecycle routes, R5 write fence, old replay continuity, session-key proof, full business parity and rollback). R5 must remain active during preparation. Historic replay keys remain in Script Properties, and the previously verified private new150 backup does not authorize deletion.
