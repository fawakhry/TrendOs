# TrendOS T12 — 128 candidate ROW POSITIONS: D1 recovery plan review (2026-09-23)

**Status: GitHub-only PLAN / NO LIVE WRITE APPROVAL / NO RUN.** Prepared after canonical journal Entry 119 on isolated branch `cloud-migration-v3-t12-order-create-ci-20260919`. This document is a decision gate, NOT an executable recovery command or evidence of current production parity. Original Google Apps Script/Sheets remain the exclusive REAL business Order CREATE / numbering authority. Do not deploy a Worker, run R5, alter triggers, Script Properties, real Sheets, production D1 or real order counters based on this document.

## 1. Available evidence (limited to screenshot-provided aggregate, not a fresh write-set)

Owner-provided ChatGPT Work screenshot after one explicitly approved `trendosD1TargetedRecoveryPreflightReadOnly20260919` invocation (canonical Entry 117) reports:
- Current Orders `الأوردرات`: `sourceRowCount=705`, `d1BaseRowCount=652`, 11 different existing row positions + 53 source-only tail positions = **64 candidates**, 0 unexpected, 0 duplicate.
- Current Lines `بنود الأوردرات`: `sourceRowCount=761`, `d1BaseRowCount=708`, 11 different existing row positions + 53 source-only tail positions = **64 candidates**, 0 unexpected, 0 duplicate.
- **128 candidate positions across TWO distinct tables**, not 128 unique business orders. Header positions may be included by source function's row-number model. Work summary reported source/catalog stable during this read and structural delta preconditions passed.
- This proves NO approved/committed repair. Actual differences can grow with live order activity. Exact Work runtime JSON, size of replacement row payload, individual flags and `scriptProperties.projectedTotalBytesApproxAfterBaselineReplace` are not available from screenshot. Current archived Orders/Lines, previous Sep21 R5 03:23 root exception and actual transaction disposition remain UNKNOWN. Never rerun the one-time authorized preflight merely for formatting.

## 2. Why existing production-write shortcuts fail today (verified repository code, not a production test)

| Existing file / path | Existing fail-closed cap or gap | Plan decision |
|---|---|---|
| `cloudflare-d1/t12-preview/t12-d1-targeted-recovery-plan-v1.mjs` + `t12-d1-guarded-recovery-batch-v1.mjs` | Each rejects a **total candidate count >64**; current screenshot has 128. The isolated batch uses exact pre-image CAS and one cross-tab D1 transaction but its capacity/contract does NOT admit this snapshot. | NO invocation against live data, and do not simply raise a number. |
| `cloudflare-d1/t12-preview/t12-d1-direct-cas-bounded-catchup-20260920.gs` | **64 total candidates**, **max 5 growth per tab**, 250000-byte Apps Script request budget. Observed growth is 53/tab, >5. | Do not add this function to original Head or call it. |
| `cloudflare-d1/t12-preview/t12-d1-direct-snapshot-bounded-catchup-draft-20260919.gs` | `LIVE_WRITE_AUTHORIZED=false`, 80 changed total / 5 growth per tab, and would call generic `/v1/mirror/delta` if ungated. | Do not turn gate on or use it to bypass caps. |
| `cloudflare-d1/src/mirror-delta-gate.mjs` | Checks catalog row counts/contiguity but not exact old row images in a single transaction; its code has unconditional row UPSERT and a conditional DELETE for truncation. | Never use this endpoint for a blind catch-up. |
| `cloudflare-d1/src/r4-guarded-recovery-production.mjs` | Repository candidate route exists and imports same 64-cap batch builder; deployment/enable/binding cannot be inferred from code. | No production route action, enable-flag modification or call. |
| `cloudflare-d1/t12-preview/r5-orders-periodic-bound-appsscript-candidate.gs` | Max 64 candidates / 5 growth per tab, old uncertain abort/self-disarm. | No R5 tick/start/trigger restoration. |
| `cloudflare-d1/t12-preview/t12-d1-guarded-recovery-preview-handler-v1.mjs` | Isolated TEST-DB-only and deliberately not a live route. | Test provenance is not live authorization. |

The 128-position snapshot cannot be safely "fixed" by splitting independently into two 64-position tab writes: cross-tab atomicity and source snapshot coherence would be lost. Creating a second/duplicate helper or rerunning an old R5 tick is NOT an alternative.

## 3. Mandatory evidence gates BEFORE even proposing a distinct production write

**P0 — source/account/operator fence (read-only or offline only).** Confirm original production Google workbook/Apps Script project, actual business Cloudflare Worker → intended production D1 binding, active authorized operator and current trigger state from fresh evidence; do not reveal IDs/secrets in public journal. Verify no unrecognized competing D1 mirror writer (legacy V1/V2, scheduled R5, enrichment or manual tool), and distinguish deployed live Worker bytes/flags from repo candidate code. No alteration to Google Order CREATE/numbering; real-order traffic may continue. If identity, ownership or competing writer is unresolved, STOP.

**P1 — bounded and fresh coherent row-level proposal.** Existing screenshot is only counts; a later operation must privately capture a **new complete** source and D1 rows/catalog under a deliberately approved quiet window and Script Lock, with before/after source fingerprint and remote catalog stability. Revalidate sheet IDs, widths, order/line headers, 1-based contiguous row numbers, status/note and **row-by-row expectedBefore** for changed existing positions; tail appends must be absent in D1 at commit. An unexpected D1 row, duplicate, deleted/reordered source row, schema drift, truncated page, unstable snapshot or a source gap not equal to tail growth is **STOP**. Do not calculate a real write payload from old 128 counts or expose private row images in a GitHub commit/chat.

**P2 — new isolated code and capacity qualification, NOT a live experiment.** If owner later asks for implementation, design and test a **separate** production-write-disabled candidate with explicit reviewed maximum candidate count that can accommodate actual fresh counts or STOP, total encrypted/authenticated request-body and D1 statement/time budgets, per-tab tail-growth bound and source-lock duration. Existing 64/5 thresholds are a prohibition, not suggested targets to patch upward without measurements. Verify actual current remote endpoint, request limits, D1 batch statement cap/transaction behavior and Apps Script runtime timeout through controlled TEST evidence; use dedicated TEST D1 only with fabricated rows for failure-injection CI. Check row identity, JSON schema, exact cross-tab atomicity, expectedBefore CAS including live catalog invariants at the **transaction boundary**, duplicate append conflicts, concurrent conflicting writer, post-commit error/timeout, and no leak in sanitized receipts. An isolated PASS is not production safety proof.

**P3 — scope and local storage decision.** Write proposal must enumerate only current Orders/Lines row positions (not customer entities or archived tabs), no DELETE, no order-number/new-order creation, no trigger/flag/enrichment changes, no Sheets writes and NO Apps Script Script Properties baseline overwrite as a side effect. If a later baseline update is desired, separately review current property quota, proposed chunk count/bytes and idempotency/retention strategy; screenshot shows no trustworthy capacity values and Sep19 property-quota cleanup was not permanent. Do not rerun the 150 replay-key deletion, use old `NEXT_ORDER_NO=4308` as a seed, or fall back to the V2 full rebase. Determine archived-tab freshness with a separately consented **read-only** evidence plan; current-tab recovery alone does NOT qualify archive or Cloud CREATE cutover.

**P4 — explicit owner write checkpoint (currently NOT GRANTED).** Present the exact, NEW operator-readable scope and limits: source/remote snapshot time, both tab names, candidate and growth counts, row identity/mismatch counts, max body/statement/lock budget, expected D1 binding, rollback and postflight method, any enabled route/flag, and how real order traffic is preserved. Request separate owner approval for the *one identified* live D1 transaction and any necessary Head/Worker config deployment independently. Do NOT infer consent from `نفذ` after Entry 118: that message followed an offer to REVIEW the plan and did not specify execution of a live write. If any gate absent, BLOCKED.

**P5 — hypothetical execution contract AFTER all approvals and tested code only.** Revalidate P0–P3 immediately before mutation. Submit only a freshly prepared, explicit cross-tab atomic transaction with private CAS preimages; never independent per-tab writes, NO blanket UPSERT and NO DELETE. On lost POST response, timeout or post-commit exception, mark **outcome UNKNOWN**, prohibit automatic/manual blind retry, and reconcile with fresh GET-only row parity before any compensating operation. A successful HTTP/commit receipt alone is NOT source↔D1 content parity and cannot authorize trigger restart. A rollback cannot be a blind restore over later real business writes; any compensating write needs a new coherent snapshot and separate approved CAS/fencing protocol.

**P6 — separate final-read and automation/cutover gates.** After a hypothetical successful remote transaction, use a **separately authorized** read-only full current-source/D1 row-content parity and source after-fingerprint, both catalog counts/dimensions, 0 mismatches/missing/unexpected/duplicates, plus record any source drift after the captured write. If archive, local baseline quota/capacity or concurrent writer issues remain unresolved, keep sync paused. Orders recurring sync, enrichment recurring sync, and Cloud CREATE transfer are **three independent later change decisions** with separate rollback, live qualification and explicit owner approvals. Do not alter Google's order allocator by inference.

## 4. Current STOP status and only safe next engineering deliverable

As of the Entry 117 screenshot: **production write BLOCKED** by existing 64-candidate/5-growth guards and unqualified revised atomic protocol; exact payload/property headroom/archive and fresh write-time snapshots UNKNOWN. NEXT GitHub-only engineering work may prepare isolated fabricated-data tests/spec for a NEW bounded 128+-capable transaction with proper verified limits and failure-injection (including transaction/commit-unknown and postflight), but may not alter existing production candidate limits or enable/routes, and must log each test FAIL/PASS under new Entries. If owner prefers stopping, no extra live read is required at this checkpoint. Explicit narrow permissions must precede any later extra live audit or mutation.

## 5. Evidence index

- Canonical chronological record: `TRENDOS_T12_CLOUDFLARE_TEST_EXECUTION_JOURNAL_2026-09-21.md`, Entries 117–119.
- Existing foundational protocol: `TRENDOS_D1_PAUSED_SYNC_RECOVERY_PROTOCOL_2026-09-19.md` G0–G4.
- Source files and exact GitHub blob SHAs are preserved in Entry 119; review these actual branch files rather than substituting another old ZIP/JSON.
- Most recent isolated preflight CI `35863725111` SUCCESS applies ONLY to its older test commit `023d0a9e380e934d23f6cdaa92c6404006fc7bf8`. **No new tests/CI/deployment or live D1 operations** were performed to prepare this document.

**Document status: PREPARED; owner write authorization: NO; live repair: NOT RUN.**


## P2 qualification update — isolated current-shape regression (2026-09-23)

GitHub-only test evidence now protects the existing STOP boundary. Commit `3a34f0c6f9503fed099b988330174c4a8dd9f01b` changed only `tests/t12_d1_targeted_recovery_plan_v1.test.mjs` to add a fabricated two-tab fixture with 11 changed existing + 53 tail appends per tab = 64 candidates/tab = 128 total, matching only the aggregate SHAPE from Entry 117 (no real row values/IDs). The current trusted planner rejects this exact shape with `R4_PLAN_ABORT_CANDIDATE_BUDGET`; isolated CI run `35873583458`, job `107223646952`, completed SUCCESS. Canonical journal Entry 122 records full provenance.

This is a **negative safety qualification**, not evidence that 128 can be safely written. Remaining P2 work is unchanged: any new 128+-capable candidate must stay isolated/unrouted/default-off and separately prove statement/body/runtime budget, exact pre-image CAS, cross-tab atomic rollback, concurrent-write rejection and commit-unknown/no-retry behavior using fabricated data and dedicated TEST-D1 evidence before a production proposal. Do not weaken existing 64/5 guards in-place merely to accommodate the old screenshot counts.


## P2 budget update — isolated 128-shape CAPACITY proof, NOT transaction qualification (Entry 123 / 2026-09-23)

A new **pure/NO-IO counts-only** isolated envelope was added at `cloudflare-d1/t12-preview/t12-d1-128-recovery-capacity-envelope-v1.mjs` and synthetic negative tests at `tests/t12_d1_128_recovery_capacity_envelope_v1.test.mjs`. Exact CI commit `ab2bfc6ada41f2072a80549c32edbe4d6fea3074`, isolated run `35877476059` **SUCCESS**, canonical Entry 123 `9594c7c375845930b74359511012bac139f45bcb`. For screenshot's 128 total candidate row positions, original guarded V1 shape yields **2 catalog guards + 128 row CAS statements + 2 catalog advances = 132 SQL statements** in one batch. Cloudflare's public D1 limits page `https://developers.cloudflare.com/d1/platform/limits/` states **50 queries per Worker invocation on Free / 1000 on Paid** and a 30-second batch runtime limit; `https://developers.cloudflare.com/d1/worker-api/d1-database/#batch` documents transaction rollback on statement failure. Real account's plan, this Worker's actual query budget, binding/route deployment, real private row request bytes, SQL bind count/statement byte length and live runtime have **not been measured or verified**. Current V1 row-per-statement design is above the Free published query budget and hypothetically within Paid's, with no real suitability conclusion; Free/Paid plan is NOT known.

The new module computes only aggregate feasibility and **always** reports `productionWriteAuthorized:false`; it builds no SQL, performs no database calls, cannot be imported by production routes under its isolated test and does not alter existing 64-total/5-tail guards. It is NOT a redesigned 128-capable CAS writer; the isolated CI result is NOT an actual D1 batch/rollback or complete postflight parity proof.

**New next design gate:** Before proposing an actual write-candidate, either establish the actual Workers account plan/available query budget as fresh read-only operator evidence, OR design a packed single-batch CAS transaction whose SQL statement count meets published Free per-invocation limits **without** bypassing atomicity or preimage guards. Either path needs separately testable per-SQL 100 bound parameters, 100 KB SQL length and <=30s batch runtime budgets, full source/mirror coherence, zero unexpected/duplicates and no independent per-tab partial writes; test with fabricated rows and dedicated TEST D1 only after its own operator scope. No live D1/Sheets/Properties/trigger/Worker mutation or automatic retry authorized.
